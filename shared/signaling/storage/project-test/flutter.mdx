<PlatformWrapper platform="flutter">

3. In your IDE, open `main.dart`, and update `appId`, `channelName` and `token` with the values for your temporary token.

4. Connect a test device to your development device.

5. In your IDE, click *Run app* or execute `flutter run lib/main.dart`. A moment later you see the project installed on your device.

    If this is the first time you run the project, grant microphone and camera access to your <Vpl k="CLIENT" />.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
6. Select an option and click **Join** to start a session. 
    * When you join as a **Host**, the local stream is published and played in the <Vpl k="CLIENT" />. 
    * When you join as **Audience**, the remote stream is subscribed and played.
</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
6. Click **Join** to start a call.
</ProductWrapper>
    
    You see your <Vpl k="CLIENT" /> connects to the <Vg k="AGORA_BACKEND" /> from a restricted network environment using the cloud proxy service. 

</PlatformWrapper>

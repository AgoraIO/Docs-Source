---
title: Agora
sidebar_position: 2
platform_selector: false
description: >
 Integrate Agora ASR into Conversational AI Engine.
---

import ParameterList, { Parameter } from '@site/src/components/rest-api/ParameterList';

Agora provides built-in automatic speech recognition optimized for real-time communication, offering seamless integration with low latency and reliable performance for conversational AI applications.

### Sample configuration

The following example shows a starting `asr` parameter configuration you can use when you [Start a conversational AI agent](../../rest-api/join).

```json
"asr": {
  "vendor": "agora",
  "language": "en-US"
}
```

### Key parameters

<ParameterList title="asr" required={true}>
 <Parameter name="vendor" type="string" required={true}>
 ASR provider. Set to `agora` to use Agora's built-in speech recognition service.
 </Parameter>
 <Parameter name="language" type="string" required={true}>  
 The language code for speech recognition. 
 </Parameter>
</ParameterList>


import CloneReference from '@docs/shared/common/project-test/clone-project.mdx';

<PlatformWrapper platform={["ios","macos","android"]}>
1. [Generate a temporary token](../get-started/manage-agora-account#generate-a-temporary-token) in <Vg k="CONSOLE" /> if required.

1. In your browser, navigate to the <Link target="_blank" to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link> and update _App ID_, _Channel_, and _Token_ with the values for your temporary token, then click **Join**.

<CloneReference />

4. In the reference <Vpl k="CLIENT" />, update `appId` and `rtcToken` in `config.json` with the values from <Link target="_blank" to="{{global.AGORA_CONSOLE_URL}}"><Vg k="CONSOLE" /></Link>.

5. Follow the steps in the project's README for setup.

</PlatformWrapper>
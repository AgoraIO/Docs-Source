<PlatformWrapper platform="ios">


3. In the `ViewController`, update `appID`, `channelName`, and `token` with the values from <Vg k="CONSOLE" />.

1. Edit the `AgoraAudioFrameDelegate` definition to add code that processes the raw audio data you receive in the following callbacks

    * `onRecord`: Gets the captured audio frame data
    * `onPlaybackAudioFrame`: Gets the audio frame for playback

1. Run your <Vpl k="CLIENT" /> using either a physical or a simulator iOS device.

    If this is the first time you run the project, grant microphone access to your <Vpl k="CLIENT" />.

1. Press **Join** to connect to the same channel as your web demo. 

    You hear the processed audio feed published from the web app.

</PlatformWrapper>
import ImportLibraries from '@docs/assets/code/video-sdk/authentication-workflow/import-library.mdx';
import FetchToken from '@docs/assets/code/video-sdk/authentication-workflow/fetch-token.mdx';
import JoinChannel from '@docs/assets/code/video-sdk/authentication-workflow/join-channel.mdx';
import EventHandler from '@docs/assets/code/video-sdk/authentication-workflow/event-handler.mdx';
import RenewToken from '@docs/assets/code/video-sdk/authentication-workflow/renew-token.mdx';

<PlatformWrapper platform="android, ios, macos, unity, react-js, web">

### Import the <Vpd k="SDK" /> classes and interfaces:

    <ImportLibraries/>

### Fetch a token from the server

    Use an HTTP request to retrieve an authentication token for a specific channel from the token server.

    <FetchToken/>

### Join the channel using the new token

    Retrieve a token from the token server and use it to join the channel.
    
    <JoinChannel/>

### Set up an event handler to fetch and renew the expired token

    A token expires either after the expire time specified in the call to the token server or, if the time is
    not specified, after 24 hours. When the token is about to expire your <Vpl k="CLIENT"/> receives an event notification. To continue the current session, fetch a new token and call `renewToken`.

    <EventHandler/>

<PlatformWrapper platform={["unity", "web"]}>
### Renew the expired token

    <RenewToken/>
</PlatformWrapper>

</PlatformWrapper>

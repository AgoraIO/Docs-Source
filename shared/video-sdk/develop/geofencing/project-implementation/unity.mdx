import * as data from '@site/data/variables';

<PlatformWrapper platform="unity">
### Implement the geofencing logic

1. **Set the <Vg k="AGORA_BACKEND"/> region**

    ```csharp
    public override void SetupAgoraEngine()
    {
        // Set the region of your choice.
        region = AREA_CODE.AREA_CODE_CN;

        base.SetupAgoraEngine();

    }
    ```
    Now, when <Vpd k="SDK" /> fails to connect to the specified region <Vg k="AGORA_BACKEND"/>, it throws an error instead of connecting to the <Vg k="AGORA_BACKEND"/> located in other regions.

    For more details, see:

    - <Link to="{{Global.API_REF_UNITY_ROOT}}/rtc_api_data_type.html#enum_areacode">AREA_CODE</Link>

    - <Link to="{{Global.API_REF_UNITY_ROOT}}/rtc_api_data_type.html#class_rtcengineconfig">RtcEngineContext</Link>

    - <Link to="{{Global.API_REF_UNITY_ROOT}}/class_irtcengine.html#api_irtcengine_initialize">Initialize</Link>
</PlatformWrapper>
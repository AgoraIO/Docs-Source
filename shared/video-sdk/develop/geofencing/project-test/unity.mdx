<PlatformWrapper platform="unity">
3. In Unity Editor, in your script file, update `_appID`, `_channelName` and `_token` with the values for your temporary token.

4. In Unity Editor, click **Play**. A moment later you see the <Vpl k="CLIENT" /> running on your device.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
5. Select a role using the toggle buttons then click **Join** to start the event. Now, you can see yourself on the device screen and talk to the remote user using your <Vpl k="CLIENT" />.
</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
5. Click **Join** to start a call.
</ProductWrapper>

6. In your script file,
    replace:

    ``` csharp
    RtcEngineContext context = new RtcEngineContext(_appID, 0, true,
    CHANNEL_PROFILE_TYPE.CHANNEL_PROFILE_LIVE_BROADCASTING,
    AUDIO_SCENARIO_TYPE.AUDIO_SCENARIO_DEFAULT, AREA_CODE.AREA_CODE_NA);
    ```
    with
    ``` csharp
    RtcEngineContext context = new RtcEngineContext(_appID, 0, true,
    CHANNEL_PROFILE_TYPE.CHANNEL_PROFILE_LIVE_BROADCASTING,
    AUDIO_SCENARIO_TYPE.AUDIO_SCENARIO_DEFAULT, AREA_CODE.AREA_CODE_JP);
    ```
    Now, you can connect to a remote user on the device. It means your <Vpl k="CLIENT" /> has made a connection with the specified region <Vg k="AGORA_BACKEND" />.
</PlatformWrapper>
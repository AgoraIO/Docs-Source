<PlatformWrapper platform="web, react-js">
1.  **Create a new React project**

    In a terminal window, execute the following command:

    ```bash
    npm create vite@latest <app-name> --template react-ts
    ```

    In `Select a framwork`, choose `React`. In `Select a Variant`, choose `Typescript`. This installs [Vite](https://vitejs.dev/) and scaffolds a new React project with Typescript.

2.  **Install the <Vg k="UIK" />**

    In the terminal window, navigate to the `<app-name>` directory:

    ```bash
    cd <app-name>
    ```

    Execute the following command to install the <Vg k="UIK" />:

    ```bash
    npm i agora-react-uikit
    ```

</PlatformWrapper>

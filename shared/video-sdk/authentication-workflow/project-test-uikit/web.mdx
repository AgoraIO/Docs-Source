<PlatformWrapper platform="web">
        
3.  In your  <Vpl k="CLIENT" / >, Update the `appID` in the declarations to the value from Agora Console.

1.  Set the `token` variable to an empty string in the declarations.

1.  Set `channel` to the same string you specified in the web demo app.

1.  Update `tokenUrl` to the base URL of your token server. For example, `https://<app-name>.herokuapp.com`.

1.  Start the dev server.

    In the terminal, execute the following commands.

    ```bash
    cd <app-name>
    npm start
    ```

    Use the URL displayed in the terminal to open the app in your browser.
</PlatformWrapper>

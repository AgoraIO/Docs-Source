<PlatformWrapper platform="react-js">

3. In `src/config.json`, update the values of `appID`, `channelName`, and `token` with the values for your temporary token.

1. In Terminal run the following command to start a proxy server:

    ```bash
    node ./utils/proxy.js
    ```

1. In another Terminal instance run the following command:

    ```terminal
    yarn dev
    ```

    The project opens in your default browser.

1. In the dropdown, select a sample you want to run and test the code.

</PlatformWrapper>
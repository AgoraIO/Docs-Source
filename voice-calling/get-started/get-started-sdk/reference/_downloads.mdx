<PlatformWrapper platform="android">

### Sample project
Agora provides an open source interactive live broadcast example project [JoinChannelAudio](https://github.com/AgoraIO/API-Examples/tree/4.0.0-beta/Android/APIExample/app/src/main/java/io/agora/api/example/examples/basic) on GitHub for your reference.

### Automatically integrate the SDK with Jitpack

1.  In `/Gradle Scripts/build.gradle(Project: <projectname>)`, add JitPack to the list of repositories with the following lines:

```
all projects {
		repositories {
		...
		maven { url 'https://www.jitpack.io' }
		}
}
```

2.  In `/Gradle Scripts/build.gradle(Module: <projectname>.app)`, integrate the Agora Voice SDK into your Android project by adding the following lines:

```
dependencies {
		...
		// Fill in a specific SDK version number (get the latest version from the release notes). 
		// For example, 4.0.0.beta.1
		implementation 'com.github.agorabuilder:agora-voice-beta:4.0.0.beta.1'
}
```

### Manual <Vpd k="PRODUCT" /> integration

1.  Go to [SDK Downloads](https://docs.agora.io/en/All/downloads?platform=All%20Platforms), download the latest version of the <Vpd k="SDK" />, then extract the files from the downloaded SDK package to a local directory.

2.  Copy the following files and folders from the `<unzipped_package>/rtc/sdk` to your project:

| File or folder            | Path in your project      |
|---------------------------|---------------------------|
| Files                     |                           |
| `agora-rtc-sdk.jar`       | `/app/libs/`              |
| Folders                   |                           |
| `arm-v8a`                 | `/app/src/main/jniLibs/`  |
| `armeabi-v7a`             | `/app/src/main/jniLibs/`  |
| `x86`                     | `/app/src/main/jniLibs/`  |
| `x86_64`                  | `/app/src/main/jniLibs/`  |
| `high_level_api/include`  | `/app/src/main/jniLibs/`  |


3.  Select `Project Files/app/libs/agora-rtc-sdk.jar` file on navigation bar, right-click and then select `add as a library`.

</PlatformWrapper>

<PlatformWrapper platform="ios">

### Complete source code

1.	For a more complete example, see the [open source <Vpd k="PRODUCT" /> example project](https://github.com/AgoraIO/API-Examples/tree/master/iOS) on GitHub.

### Install <Vpd k="SDK" /> using CocoaPods

1.	Ensure that you have installed *CocoaPods* before performing the following steps. See the installation guide in [Getting Started with CocoaPods](https://guides.cocoapods.org/using/getting-started.html#getting-started).

2.	In Terminal, navigate to the project path, and run the `pod init` command to create a `Podfile` in the project folder.

3.	Open the `Podfile`, delete all contents, and input the following codes. Remember to replace `Your App` with the target name of your project and replace `version` with the version of the SDK that you want to integrate.

	``` terminal
	# platform :ios, '9.0'
	target 'Your App' do
		pod 'AgoraAudio_iOS', 'version'
	end
	```

4.	Return to Terminal, and run the `pod install` command to install <Vpd k="SDK" />. Once you successfully install the SDK, it shows `Pod installation complete!` in Terminal, and you can see an `xcworkspace` file in the project folder.

5.	Open the generated `xcworkspace` file.

</PlatformWrapper>

<PlatformWrapper platform="web">

### API references

1.	[AgoraRTC.createClient](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iagorartc.html#createclient)
2.	[join](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iagorartcclient.html#join)
3.	[createMicrophoneAudioTrack](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iagorartc.html#createmicrophoneaudiotrack)
4.	[publish](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iagorartcclient.html#publish)
5.	[IAgoraRTCRemoteUser](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iagorartcremoteuser.html)
6.	[play](https://docs.agora.io/en/Video/API%20Reference/web_ng/interfaces/iremoteaudiotrack.html#play)

### Complete source code

For a more complete example, see the [open source <Vpd k="PRODUCT" /> example project](https://github.com/AgoraIO/API-Examples-Web) on GitHub.

### Manual <Vpd k="SDK" /> integration

1.	Go to [SDK Downloads](https://docs.agora.io/en/Video/downloads?platform=Web) and download the latest version of <Vpd k="SDK" />. 

2.	Extract the files from the downloaded package to a local directory.

3.	Add the following code to the line before the `<style>` tag in your project:

	``` html
	<script src="./AgoraRTC_N-4.11.1.js"></script>
	```

</PlatformWrapper>
<PlatformWrapper platform="windows">
3.  Set the `appID`, `channelName` and `token` with the values for your temporary token.

1.  Run your <Vpl k="CLIENT"/>, then wait a few seconds until the installation is complete.

1. Select an option and click the **Join** button to start a session. When you join as a **Host**, the local video is published and played in the <Vpl k="CLIENT"/>. When you join as **Audience**, the remote stream is subscribed and played.


</PlatformWrapper>
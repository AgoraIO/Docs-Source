<PlatformWrapper platform="unity">

3. In your script file, update `_appID`, `_channelName`, and `_token` with the values for your temporary token.

4. Edit the `AudioFrameObserver` definition by adding code that processes the raw audio data you receive in the following callbacks:

    - `OnRecordAudioFrame`: Gets the captured audio frame data

    - `OnPlaybackAudioFrame`: Gets the audio frame for playback

5.  In **Unity Editor**, click **Play**. A moment later you see the <Vpl k="CLIENT" /> installed on your device.

6. Click **Join** to hear the processed audio feed from the web app.

</PlatformWrapper>

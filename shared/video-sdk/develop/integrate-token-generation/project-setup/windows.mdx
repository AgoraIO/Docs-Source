<PlatformWrapper platform="windows">

Set up [OpenSSL](https://www.openssl.org/)  and [zlib](https://www.zlib.net/) in your project.

**Note** - If you face any issue to install `OpenSSL` and `zLib` libraries in your project, you can install it through [vcpkg](integrate-token-generation?platform=windows#install-required-library-through-vcpkg).

</PlatformWrapper>
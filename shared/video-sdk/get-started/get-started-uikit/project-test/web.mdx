<PlatformWrapper platform="web, react-js">

3.  In `<app-name>/src/App.tsx`, update `appID`, `channel` and `token` with the values for your temporary token.

4.  Start the dev server.

    Navigate to the project folder and execute the following command in the terminal:

    ```bash
    npm run dev
    ```

    Use the URL displayed in the terminal to open the <Vpl k="CLIENT" /> in your browser.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
5. Select a user role using the radio buttons.

6.  To connect to a channel, click **Join**.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. To connect to a channel, click **Join**.
</ProductWrapper>

    If this is the first time you run your app, grant microphone and camera access to
    your <Vpl k="CLIENT" />.

Now, you can see yourself in the local view and talk to the other user in your <Vpl k="CLIENT" />.

</PlatformWrapper>

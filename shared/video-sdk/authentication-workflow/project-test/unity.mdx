<PlatformWrapper platform="unity">

3. **Configure the project**:

    In `Assets/agora-manager/config.json`, Replace:

    * `appId` - with your app ID from <Link to="{{Global.AGORA_CONSOLE_URL}}"><Vg k="CONSOLE" /></Link>.
    * `channelName` - an empty string.
    * `token` - an empty string.
    * `serverUrl` - with the base URL for your token server.
        For example: `https://agora-token-service-production-yay.up.railway.app`.
    * `tokenExpiryTime` - with an integer denoting the number of seconds after which a token expires.

1.  **Run the <Vpl k="NAME" /> reference app**:

    In Unity, click **Play**. You see the <Vpl k="CLIENT" /> running on your device.

1. **Join a Channel**:
    <ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
    1. Set **Channel name** to the name you used in the <Link to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

    1. Connect to a channel, click **Join**.

    1. To change the user role, use the toggle buttons.

    </ProductWrapper>
    <ProductWrapper product="video-calling">
    1. Set **Channel name** to the name you used in the <Link to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

    1. Connect to a channel, click **Join**.

    </ProductWrapper>

</PlatformWrapper>
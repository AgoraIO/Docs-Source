import NotAvailable from '@docs/shared/common/not-available.mdx'
import videoURL from '@docs/assets/images/video-sdk/preload.mp4';
import Prerequisites from '@docs/shared/common/prerequities.mdx';
import ProjectSetup from '@docs/shared/video-sdk/best-practices/preload-channels/project-setup/index.mdx';
import Implementation from '@docs/shared/video-sdk/best-practices/preload-channels/project-implementation/index.mdx';

<PlatformWrapper platform={['web','android','ios','macos']}>
Preloading channels ensures that content is displayed almost instantly when viewers join a channel, reducing any noticeable loading times. This feature enhances the viewing experience by ensuring smoother transitions and minimizing interruptions.

The following video demonstrates the channel preload experience:

<video src={videoURL} controls style={{ width: '100%', height: 'auto' }} loop>
Your browser does not support the <code>video</code> element.
</video>

The following figure illustrates the best practice when preloading channels:

![preload channels best practice](/images/video-sdk/preload-channels-best-practice.png)

## Prerequisites

<Prerequisites />

## Project setup

<ProjectSetup />

## Implementation

Follow these steps to implement channel preloading:

<Implementation />

</PlatformWrapper>

<PlatformWrapper notAllowed={['web','android','ios','macos']}>
<NotAvailable />
</PlatformWrapper>

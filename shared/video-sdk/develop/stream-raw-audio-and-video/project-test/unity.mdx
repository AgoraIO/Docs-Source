<PlatformWrapper platform="unity">

1.  Generate a temporary token in Agora Console.

2. In your browser, navigate to the <Link target="_blank" to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link> and update _App ID_, _Channel_, and _Token_ with the values for your temporary token, then click **Join**.

3. In **Unity Editor**, in `Assets/Agora-Unity-<Vg k="VSDK" />-SDK/Code/JoinChannelVideo.cs`, update `_appID`, `_channelName`, and `_token` with the values for your temporary token.

4.  In **Unity Editor**, click **Play**. A moment later you see the <Vpl k="CLIENT" /> installed on your device.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
5. Select a role using the toggle buttons and click **Join** to start an interactive live streaming session.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. Click **Join** to start a call.
</ProductWrapper>

6.  Test processing of raw video data.

    Press **Zoom In**. You see that the local video captured by your device camera is cropped and scaled to give a zoom-in effect. The processed video is displayed both locally and remotely. Pressing the button again stops processing of raw video data and restores the original video.

7.  Test processing of raw audio data.

    Edit the `AudioFrameObserver` definition by adding code that processes the raw audio data you receive in the following callbacks:

    - `OnRecordAudioFrame`: Gets the captured audio frame data

    - `OnPlaybackAudioFrame`: Gets the audio frame for playback

</PlatformWrapper>

---

title: LocalUserObserver Class
---

<span className="index-group-localuserobserver" style={{display: 'none'}}>LocalUserObserver</span>

<span className="index-desc-localuserobserver">`LocalUserObserver` class.</span>

<h2 className="anchor index-group-item-localuserobserver index-api-onuseraudiotrackstatechanged" id="onuseraudiotrackstatechanged">OnUserAudioTrackStateChanged</h2>

```go
OnUserAudioTrackStateChanged func(localUser *LocalUser, uid string, remoteAudioTrack *RemoteAudioTrack, state int, reason int, elapsed int)
```

<span className="index-desc-onuseraudiotrackstatechanged">Triggered when the state of a remote audio track changes.</span>

#### Parameters

| Parameter | Description |
|-----------|-------------|
| `localUser` | `LocalUserObserver` class. |
| `uid` | Remote user ID. |
| `remoteAudioTrack` | Remote audio track. |
| `state` | Current state of the audio track. |
| `reason` | Reason for the state change. |
| `elapsed` | Time elapsed (in milliseconds) since the remote user joined the channel. |

{/*
<h2 className="anchor index-group-item-localuserobserver index-api-onuseraudiotracksubscribed" id="onuseraudiotracksubscribed">OnUserAudioTrackSubscribed</h2>

```go
OnUserAudioTrackSubscribed func(localUser *LocalUser, uid string, remoteAudioTrack *RemoteAudioTrack)
```

<span className="index-desc-onuseraudiotracksubscribed">Triggered when a user's audio track is subscribed.</span>

#### Parameters

| Parameter | Description |
|-----------|-------------|
| `localUser` | `LocalUserObserver` class. |
| `uid` | User ID. |
| `remoteAudioTrack` | Remote audio track. |
*/}


{/*
<h2 className="anchor index-group-item-localuserobserver index-api-onuservideotracksubscribed" id="onuservideotracksubscribed">OnUserVideoTrackSubscribed</h2>

```go
OnUserVideoTrackSubscribed func(localUser *LocalUser, uid string, info *VideoTrackInfo, remoteVideoTrack *RemoteVideoTrack)
```

<span className="index-desc-onuservideotracksubscribed">Triggered when a user's video track is subscribed.</span>

#### Parameters

| Parameter | Description |
|-----------|-------------|
| `localUser` | `LocalUserObserver` class. |
| `uid` | User ID. |
| `info` | Video track information. |
| `remoteVideoTrack` | Remote video track. |
*/}


<h2 className="anchor index-group-item-localuserobserver index-api-onstreammessage" id="onstreammessage">OnStreamMessage</h2>

```go
OnStreamMessage func(localUser *LocalUser, uid string, streamId int, data []byte)
```

<span className="index-desc-onstreammessage">Callback for receiving stream messages. This callback indicates that the local user has received a stream message sent by a remote user using the `SendStreamMessage` method.</span>

#### Parameters

| Parameter | Description |
|-----------|-------------|
| `localUser` | `LocalUserObserver` class. |
| `uid` | User ID of the sender. |
| `streamId` | Stream ID of the received message. |
| `data` | Received data. |

<h2 className="anchor index-group-item-localuserobserver index-api-onuserinfoupdated" id="onuserinfoupdated">OnUserInfoUpdated</h2>

```go
OnUserInfoUpdated func(localUser *LocalUser, uid string, userMediaInfo int, val int)
```

<span className="index-desc-onuserinfoupdated">Triggered when user media information is updated.</span>

#### Parameters

export const TableHeaderfhdnmguy2j = [
  {
    label: 'Parameter'
  },
  {
    label: 'Description'
  }
];

<Table header={TableHeaderfhdnmguy2j}>
  <Tr>
    <Td>`localUser`</Td>
    <Td>[`LocalUser`](./localuser) class.</Td>
  </Tr>
  <Tr>
    <Td>`uid`</Td>
    <Td>User ID.</Td>
  </Tr>
  <Tr>
    <Td>`userMediaInfo`</Td>
    <Td>User media information. See [`UserMediaInfo`](./apidatatype#usermediainfo) for details.</Td>
  </Tr>
  <Tr>
    <Td>`val`</Td>
    <Td>
    Whether the user has stopped sending streams.
    - `0`: Not stopped.
    - `1`: Stopped.
    </Td>
  </Tr>
</Table>
<PlatformWrapper platform="windows">


3. In `AgoraImplementationDlg.cpp`, update `appId`, `channelName`, and `token` with the values for your temporary token.

1. Download a [sample video file](\files\sample.mp4) and a [sample audio file](\files\applause.wav) to a local
  directory.

1. In `AgoraImplementationDlg.h`, update `video_path` and `audio_path` with the path to these files.

6. In Visual Studio, click **Local Windows Debugger**. A moment later you see the project running on your development device.

    If this is the first time you run the project, grant microphone and camera access to your app.

7. **Test custom video source**

    To connect a channel, click **Join**.

    You see local video from external video source in the local view of your <Vpl k ="CLIENT"/>. 

8. **Test the custom audio source**

    To connect a channel, click **Join**.

    Click <code><strong>`Play External Audio`</strong></code>  button to hear audio from the custom source (sample audio file). You can pause and play this custom audio file. Button toggles accordingly.

</PlatformWrapper>

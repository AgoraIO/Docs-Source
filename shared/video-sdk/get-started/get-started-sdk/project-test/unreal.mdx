<PlatformWrapper platform="unreal">

3. Now at your end (consider it **Local User**) make changes in **MyUserWidget.h**. Update `UMyUserWidget` class variables `appId`, `channelName` and `token` with the values of generated temporary token. Build and run the code, it opens **Unreal Editor**.

4. In **Unreal Editor**, load **Level Blueprint** (In **Content Browser**, navigate to the `Content` folder and double-click `Untitled`). Click **Play**. A moment later you see the <Vpl k="CLIENT" /> running on your development device.
   If this is the first time you run the project, grant camera and microphone access to your app.   
   
<ProductWrapper product="interactive-live-streaming">
5. Select a role using the check boxes.

6. Click **Join** to start a call. Now, you can see yourself on the device screen and talk to the remote user using your <Vpl k="CLIENT" />.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. Click **Join** to start a call. You can see yourself as well as remote user on the device screen. Now, you can start video chat to the remote user using this <Vpl k="CLIENT" />.
</ProductWrapper>

</PlatformWrapper>
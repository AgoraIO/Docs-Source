<PlatformWrapper platform="react-js">

3. In `src/App.js`, update the values of `appID`, `channelName`, and `token` with the values for your temporary token.

1. Open Terminal in the root directory of the cloned repository and run the following command:
    ```terminal
    npm start
    ```

    The project opens in your default browser.

1. Select an item from the dropdown to test the sample code.

</PlatformWrapper>
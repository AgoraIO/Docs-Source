import * as storage from '../shared/_storage.mdx'
import * as enumv from '../shared/_enumv.mdx'
import * as config from '../shared/_configuration.mdx'
import * as presence from '../shared/_presence.mdx'

### Enum

#### {enumv.areacode[props.ag_platform]}

<span className="index-desc-areacode">The region for connection, which is the region where the server the SDK connects to is located.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.areacodecn[props.ag_platform]}</code> | `0x00000001`: Mainland China. |
| <code>{enumv.areacodena[props.ag_platform]}</code> | `0x00000002`: North America. |
| <code>{enumv.areacodeeu[props.ag_platform]}</code> | `0x00000004`: Europe. |
| <code>{enumv.areacodeas[props.ag_platform]}</code> | `0x00000008`: Asia, excluding Mainland China. |
| <code>{enumv.areacodejp[props.ag_platform]}</code> | `0x00000010`: Japan. |
| <code>{enumv.areacodein[props.ag_platform]}</code> | `0x00000020`: India. |
| <code>{enumv.areacodeglob[props.ag_platform]}</code> | `0xFFFFFFFF`: Global. |

#### {enumv.channeltype[props.ag_platform]}

<span className="index-desc-channeltype">Channel types.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.channeltypemessage[props.ag_platform]}</code> | `1`: Message channel. |
| <code>{enumv.channeltypestream[props.ag_platform]}</code> | `2`: Stream channel. |
| <code>{enumv.channeltypeuser[props.ag_platform]}</code> | `3`: User Channel. |

#### {enumv.connectionreason[props.ag_platform]}

<span className="index-desc-connectionreason">Reasons causing the change of the connection state.</span>

<PlatformWrapper platform={['android', 'unity', 'linux-cpp']}>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.reasonconnecting[props.ag_platform]}</code> | `0`: The SDK is connecting with the server. |
| <code>{enumv.reasonjoinsuccess[props.ag_platform]}</code> | `1`: The SDK has joined the channel successfully. |
| <code>{enumv.reasoninterrupt[props.ag_platform]}</code> | `2`: The connection between the SDK and the server is interrupted. |
| <code>{enumv.reasonban[props.ag_platform]}</code> | `3`: The connection between the SDK and the server is banned by the server.  |
| <code>{enumv.reasonjoinfail[props.ag_platform]}</code> | `4`: The SDK fails to join the channel. When the SDK fails to join the channel for more than 20 minutes, this error occurs and the SDK stops reconnecting to the channel. |
| <code>{enumv.reasonleave[props.ag_platform]}</code> | `5`: The SDK has left the channel. |
| <code>{enumv.reasoninvalidid[props.ag_platform]}</code> | `6`: The connection failed because the App ID is not valid. |
| <code>{enumv.reasoninvalidchannel[props.ag_platform]}</code> | `7`: The connection failed because the channel name is not valid. |
| <code>{enumv.reasoninvalidtoken[props.ag_platform]}</code> | `8`: The connection failed because the token is not valid. |
| <code>{enumv.reasontokenexpired[props.ag_platform]}</code> | `9`: The connection failed because the token is expired. |
| <code>{enumv.reasonrejected[props.ag_platform]}</code> | `10`: The connection is rejected by server. |
| <code>{enumv.reasonproxychange[props.ag_platform]}</code> | `11`: The connection state changed to reconnecting because the SDK has set a proxy server. |
| <code>{enumv.reasonrenewtoken[props.ag_platform]}</code> | `12`: The connection state changed because the token is renewed. |
| <code>{enumv.reasonipchange[props.ag_platform]}</code> | `13`: The IP address of the client has changed, possibly because the network type, IP address, or port has been changed. |
| <code>{enumv.reasonlivetimeout[props.ag_platform]}</code> | `14`: Timeout for the keep-alive of the connection between the SDK and the server. The connection state changes to reconnecting. |
| <code>{enumv.reasonrejoin[props.ag_platform]}</code> | `15`: The user has rejoined the channel successfully. |
| <code>{enumv.reasonlost[props.ag_platform]}</code> | `16`: The connection between the SDK and the server is lost. |
| <code>{enumv.reasonecho[props.ag_platform]}</code> | `17`: The connection state changes due to the echo test. |
| <code>{enumv.reasonuserchangeip[props.ag_platform]}</code> | `18`: The local IP address was changed by the user. The connection state changes to reconnecting. |
| <code>{enumv.reasonsameuid[props.ag_platform]}</code> | `19`: The user joined the same channel from different devices with the same UID. |
| <code>{enumv.reasonmanyhosts[props.ag_platform]}</code> | `20`: The number of hosts in the channel has reached the upper limit. |
| <code>{enumv.reasonlicense[props.ag_platform]}</code> | `21`: License validation failure. |
|    <code>{enumv.reasoncertification[props.ag_platform]}</code>  | `22`: The server certificate verification has encountered an error.    |
| <code>{enumv.reasonstreamchannel[props.ag_platform]}</code> | `23`: The stream channel does not exist. |
| <code>{enumv.reasoninconsistentappid[props.ag_platform]}</code> | `24`: The App ID does not match the token. |
| <code>{enumv.reasonloginsuccess[props.ag_platform]}</code> | `10001`: The SDK logs in to the Signaling system. |
| <code>{enumv.reasonlogout[props.ag_platform]}</code> | `10002`: The SDK logs out from the Signaling system. |
| <code>{enumv.reasonpresence[props.ag_platform]}</code> | `10003`: Presence service is not ready. You need to call the <code>{config.login[props.ag_platform]}</code> method again to log in to the Signaling system and re-execute all operations on the SDK. |

</PlatformWrapper>

<PlatformWrapper platform={['ios']}>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.reasonconnecting[props.ag_platform]}</code> | `0`: The SDK is connecting with the server. |
| <code>{enumv.reasonjoinsuccess[props.ag_platform]}</code> | `1`: The SDK has joined the channel successfully. |
| <code>{enumv.reasoninterrupt[props.ag_platform]}</code> | `2`: The connection between the SDK and the server is interrupted. |
| <code>{enumv.reasonban[props.ag_platform]}</code> | `3`: The connection between the SDK and the server is banned by the server.  |
| <code>{enumv.reasonjoinfail[props.ag_platform]}</code> | `4`: The SDK fails to join the channel. When the SDK fails to join the channel for more than 20 minutes, this error occurs and the SDK stops reconnecting to the channel. |
| <code>{enumv.reasonleave[props.ag_platform]}</code> | `5`: The SDK has left the channel. |
| <code>{enumv.reasoninvalidid[props.ag_platform]}</code> | `6`: The connection failed because the App ID is not valid. |
| <code>{enumv.reasoninvalidchannel[props.ag_platform]}</code> | `7`: The connection failed because the channel name is not valid. |
| <code>{enumv.reasoninvalidtoken[props.ag_platform]}</code> | `8`: The connection failed because the token is not valid. |
| <code>{enumv.reasontokenexpired[props.ag_platform]}</code> | `9`: The connection failed because the token is expired. |
| <code>{enumv.reasonrejected[props.ag_platform]}</code> | `10`: The connection is rejected by server. |
| <code>{enumv.reasonproxychange[props.ag_platform]}</code> | `11`: The connection state changed to reconnecting because the SDK has set a proxy server. |
| <code>{enumv.reasonrenewtoken[props.ag_platform]}</code> | `12`: The connection state changed because the token is renewed. |
| <code>{enumv.reasonipchange[props.ag_platform]}</code> | `13`: The IP address of the client has changed, possibly because the network type, IP address, or port has been changed. |
| <code>{enumv.reasonlivetimeout[props.ag_platform]}</code> | `14`: Timeout for the keep-alive of the connection between the SDK and the server. The connection state changes to reconnecting. |
| <code>{enumv.reasonrejoin[props.ag_platform]}</code> | `15`: The user has rejoined the channel successfully. |
| <code>{enumv.reasonlost[props.ag_platform]}</code> | `16`: The connection between the SDK and the server is lost. |
| <code>{enumv.reasonecho[props.ag_platform]}</code> | `17`: The connection state changes due to the echo test. |
| <code>{enumv.reasonuserchangeip[props.ag_platform]}</code> | `18`: The local IP address was changed by the user. The connection state changes to reconnecting. |
| <code>{enumv.reasonsameuid[props.ag_platform]}</code> | `19`: The user joined the same channel from different devices with the same UID. |
| <code>{enumv.reasonmanyhosts[props.ag_platform]}</code> | `20`: The number of hosts in the channel has reached the upper limit. |
| <code>{enumv.reasonstreamchannel[props.ag_platform]}</code> | `22`: The stream channel does not exist. |
| <code>{enumv.reasonloginsuccess[props.ag_platform]}</code> | `10001`: The SDK logs in to the Signaling system. |
| <code>{enumv.reasonlogout[props.ag_platform]}</code> | `10002`: The SDK logs out from the Signaling system. |
| <code>{enumv.reasonpresence[props.ag_platform]}</code> | `10003`: Presence service is not ready. You need to call the <code>{config.login[props.ag_platform]}</code> method again to log in to the Signaling system and re-execute all operations on the SDK. |

</PlatformWrapper>

#### {enumv.connectionstate[props.ag_platform]}

<span className="index-desc-connectionstate">SDK connection states.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.sdkdisconnect[props.ag_platform]}</code> | `1`: The SDK has disconnected with the server. |
| <code>{enumv.sdkconnecting[props.ag_platform]}</code> | `2`: The SDK is connecting with the server. |
| <code>{enumv.sdkconnected[props.ag_platform]}</code> | `3`: The SDK has connected with the server. |
| <code>{enumv.sdkreconnect[props.ag_platform]}</code> | `4`: The connection is lost. The SDK is reconnecting with the server. |
| <code>{enumv.sdkfailed[props.ag_platform]}</code> | `5`: The SDK failed to connect with the server. |

#### {enumv.encryptionmode[props.ag_platform]}

<span className="index-desc-encryptionmode">Encryption mode.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.encryptionmodenone[props.ag_platform]}</code> | `0`: No encryption. |
| <code>{enumv.aesonetwoeight[props.ag_platform]}</code> | `1`: AES-128-GCM mode. |
| <code>{enumv.aestwofivesix[props.ag_platform]}</code> | `2`: AES-256-GCM mode. |

<PlatformWrapper platform={['ios']}>

#### {enumv.joinchannelfeature[props.ag_platform]}

<span className="index-desc-joinchannelfeature">The event notification types set when joining a channel. You can use bitwise operations to set multiple event notifications simultaneously.</span>

|        Value      | Description                                                |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.joinchannelfeaturenone[props.ag_platform]}</code> | `0`: No event notifications. |
|   <code>{enumv.joinchannelfeaturepresence[props.ag_platform]}</code>    | `1`: Sets the presence event notification. |
|  <code>{enumv.joinchannelfeaturemetadata[props.ag_platform]}</code>  | `1 << 1`: Sets the storage event notification.|
|  <code>{enumv.joinchannelfeaturelock[props.ag_platform]}</code>  | `1 << 2`: Sets the lock event notification.  |
|  <code>{enumv.joinchannelfeaturebequiet[props.ag_platform]}</code>  | `1 << 3`: Sets the silent mode. In this mode, the SDK behaves as follows:<li>You still receive event notifications from other users.</li><li>Event notifications related to your channel activity, such as joining or leaving a channel, and actions related to setting, getting, or deleting temporary user states, are not broadcasted to other users.</li><li>When calling the `getOnlineUsers` method, your information is not included.</li><li>When calling the `getUserChannels` method, the channels that you subscribe to in silent mode are not included.</li>  |

</PlatformWrapper>

#### {enumv.locktype[props.ag_platform]}

<span className="index-desc-locktype">Lock event type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.locktypesnap[props.ag_platform]}</code> | `1`: The snapshot of the lock when the user joined the channel. |
| <code>{enumv.locktypeset[props.ag_platform]}</code> | `2`: The lock is set. |
| <code>{enumv.locktyperemoved[props.ag_platform]}</code> | `3`: The lock is removed. |
| <code>{enumv.locktypeacquired[props.ag_platform]}</code> | `4`: The lock is acquired. |
| <code>{enumv.locktypereleased[props.ag_platform]}</code> | `5`: The lock is released. |
| <code>{enumv.locktypeexpired[props.ag_platform]}</code> | `6`: The lock expired. |

#### {enumv.loglevel[props.ag_platform]}

<span className="index-desc-loglevel">Log output levels.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.loglevelnone[props.ag_platform]}</code> | `0x0000`: No log. |
| <code>{enumv.loglevelinfo[props.ag_platform]}</code> | `0x0001`: Output the log at the `FATAL`, `ERROR`, `WARN`, or `INFO` level. We recommend you set to this value. |
| <code>{enumv.loglevelwarn[props.ag_platform]}</code> | `0x0002`: Output the log at the `FATAL`, `ERROR`, `WARN` level. |
| <code>{enumv.loglevelerror[props.ag_platform]}</code> | `0x0004`: Output the log at the `FATAL`, `ERROR` level. |
| <code>{enumv.loglevelfatal[props.ag_platform]}</code> | `0x0008`: Output the log at the `FATAL` level. |

#### {enumv.priority[props.ag_platform]}

<span className="index-desc-messagepriority">Message priority.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.highest[props.ag_platform]}</code> | `0`: Highest. |
| <code>{enumv.high[props.ag_platform]}</code> | `1`: High. |
| <code>{enumv.normal[props.ag_platform]}</code> | `4`: Normal. |
| <code>{enumv.low[props.ag_platform]}</code> | `8`: Low. |

#### {enumv.messageqos[props.ag_platform]}

<span className="index-desc-messageqos">QoS guarantee when sending topic messages.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.unordered[props.ag_platform]}</code> | `0`: Message data is not guaranteed to arrive in order. |
| <code>{enumv.ordered[props.ag_platform]}</code> | `1`: Message data arrives in order. |

<PlatformWrapper platform={['android', 'unity']}>

#### {enumv.messagetype[props.ag_platform]}

<span className="index-desc-messagetype">Message type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.binarymessage[props.ag_platform]}</code> | `0`: Binary type. |
| <code>{enumv.stringmessage[props.ag_platform]}</code> | `1`: String type. |

</PlatformWrapper>

#### {enumv.presencetype[props.ag_platform]}

<span className="index-desc-presencetype">Presence event type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.presencetypesnap[props.ag_platform]}</code> | `1`: The snapshot of the presence when the user joined the channel. |
| <code>{enumv.presencetypeint[props.ag_platform]}</code> | `2`: When users in the channel reach the setting value, the event notifications are sent at intervals rather than in real time. |
| <code>{enumv.presencetypejoin[props.ag_platform]}</code> | `3`: A remote user joined the channel. |
| <code>{enumv.presencetypeleave[props.ag_platform]}</code> | `4`: A remote user left the channel. |
| <code>{enumv.presencetypetimeout[props.ag_platform]}</code> | `5`: A remote user's connection timed out. |
| <code>{enumv.presencetypechange[props.ag_platform]}</code> | `6`: A remote user's temporary state changed. |
| <code>{enumv.presencetypeout[props.ag_platform]}</code> | `7`: The user did not enable presence when joining the channel. |


#### {enumv.linkoperation[props.ag_platform]}

<span className="index-desc-linkoperation">Operation type.</span>

|       Value      | Description                                                |
| :----------: | ---------------------------------------------------------- |
|   <code>{enumv.linkoperationlogin[props.ag_platform]}</code>  |        `0`: The user logins to the Signaling system.                  |
|   <code>{enumv.linkoperationlogout[props.ag_platform]}</code>    |      `1`: The user logouts of the Signaling system.     |
|   <code>{enumv.linkoperationjoin[props.ag_platform]}</code>    |      `2`: The user joins in a stream channel.      |
|   <code>{enumv.linkoperationleave[props.ag_platform]}</code>    |      `3`: The user leaves a stream channel.      |
|   <code>{enumv.linkoperationreject[props.ag_platform]}</code>    |      `4`: The Signaling server reject the connection.      |
|   <code>{enumv.linkoperationreconnect[props.ag_platform]}</code>    |      `5`: The SDK is automatically reconnecting to the Signaling server.      |
|   <code>{enumv.linkoperationreconnected[props.ag_platform]}</code>    |      `6`: The SDK is  reconnected to the Signaling server.      |
|   <code>{enumv.linkoperationheatbeattimeout[props.ag_platform]}</code>    |      `7`: The Signaling server does not receive the heartbeat packet within the specified timeout period.      |
|   <code>{enumv.linkoperationservertimeout[props.ag_platform]}</code>    |      `8`: The Signaling server has timed out.      |
|   <code>{enumv.linkoperationnetwork[props.ag_platform]}</code>    |      `9`: The network status changes.     |

#### {enumv.linkstate[props.ag_platform]}

<span className="index-desc-linkstate">Link state type.</span>

|       Value      | Description                                                |
| :----------: | ---------------------------------------------------------- |
|   <code>{enumv.linkstateidle[props.ag_platform]}</code>  |        `0`: The init state.                  |
|   <code>{enumv.linkstateconnecting[props.ag_platform]}</code>    |      `1`: Connecting.     |
|   <code>{enumv.linkstateconnected[props.ag_platform]}</code>    |      `2`: Connected.      |
|   <code>{enumv.linkstatedisconnected[props.ag_platform]}</code>    |      `3`: Disconnected.      |
|   <code>{enumv.linkstatesuspended[props.ag_platform]}</code>    |      `4`: Suspended.     |
|   <code>{enumv.linkstatefailed[props.ag_platform]}</code>    |      `5`: Failed.      |

#### {enumv.protocoltype[props.ag_platform]}

<span className="index-desc-protocoltype">Protocol type.</span>

|       Value      | Description                                                |
| :----------: | ---------------------------------------------------------- |
|   <code>{enumv.protocoltypeall[props.ag_platform]}</code>  |        `0`: Both TCP and UDP protocols.                  |
|   <code>{enumv.protocoltypetcp[props.ag_platform]}</code>    |      `1`: Only TCP protocol.     |

#### {enumv.rtmservicetype[props.ag_platform]}

<span className="index-desc-rtmservicetype">Service type</span>

|       Value      | Description                                                |
| :----------: | ---------------------------------------------------------- |
|   <code>{enumv.rtmservicetypemessage[props.ag_platform]}</code>  |          The foundational services comprise the message channel, user channel, presence, storage, and lock services.                    |
|   <code>{enumv.rtmservicetypestream[props.ag_platform]}</code>    |      The stream channel service.      |

<Admonition type="info" title="Information">
To fully utilize all services offered by Signaling, use bitwise operations to simultaneously configure two service types.
</Admonition>



#### {enumv.proxytype[props.ag_platform]}

<span className="index-desc-proxytype">Proxy type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.proxytypenone[props.ag_platform]}</code> | `0`: Do not enable the proxy. |
| <code>{enumv.proxytypehttp[props.ag_platform]}</code> | `1`: Enable the proxy for the HTTP protocol. |
| <code>{enumv.proxytypetcp[props.ag_platform]}</code> | `2`: Enable cloud proxy for the TCP protocol. |

#### {enumv.storageeventtype[props.ag_platform]}

<span className="index-desc-storageeventtype">Storage event type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.storageeventtypesnap[props.ag_platform]}</code> | `1`: When a user subscribes to channel metadata or user etadata for the first time, or joins a channel, the local user receives notifications of this type of event. |
| <code>{enumv.storageeventtypeset[props.ag_platform]}</code> | `2`: Occurs when calling <code>{storage.setchannel[props.ag_platform]}</code> or <code>{storage.setuser[props.ag_platform]}</code>.<Admonition type="caution" title="Caution">This event only occurs in incremental data update mode.</Admonition> |
| <code>{enumv.storageeventtypeupdate[props.ag_platform]}</code> | `3`: Occurs when calling methods to set, update, or delete the channel metadata or user metadata. |
| <code>{enumv.storageeventtyperemove[props.ag_platform]}</code> | `4`: Occurs when calling <code>{storage.removechannel[props.ag_platform]}</code> or <code>{storage.removeuser[props.ag_platform]}</code>.<Admonition type="caution" title="Caution">This event only occurs in incremental data update mode.</Admonition> |

#### {enumv.storagetype[props.ag_platform]}

<span className="index-desc-storagetype">Storage type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.storagetypeuser[props.ag_platform]}</code> | `1`: User metadata event. |
| <code>{enumv.storagetypechannel[props.ag_platform]}</code> | `2`: Channel metadata event. |

<PlatformWrapper platform={['ios']}>

#### {enumv.subscribechannelfeature[props.ag_platform]}

<span className="index-desc-subscribechannelfeature">The event notification types set when subscribing to a channel. You can use bitwise operations to set multiple event notifications simultaneously.</span>

|       Value     | Description                                               |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.subscribechannelfeaturenone[props.ag_platform]}</code> | `0`: No event notifications  |
|   <code>{enumv.subscribechannelfeaturepresence[props.ag_platform]}</code>    | `1`: Sets the presence event notification. |
|  <code>{enumv.subscribechannelfeaturemetadata[props.ag_platform]}</code>  | `1 << 1`: Sets the storage event notification.|
|  <code>{enumv.subscribechannelfeaturemessage[props.ag_platform]}</code>  | `1 << 2`: Sets the message event notification.  |
|  <code>{enumv.subscribechannelfeaturelock[props.ag_platform]}</code>  | `1 << 3`: Sets the lock event notification.  |
|  <code>{enumv.subscribechannelfeaturebequiet[props.ag_platform]}</code>  | `1 << 4`: Sets the silent mode. In this mode, the SDK behaves as follows:<li>You still receive event notifications from other users.</li><li>Event notifications related to your channel activity, such as subscribing or unsubscribing the channel, and actions related to setting, getting, or deleting temporary user states, are not broadcasted to other users.</li><li>When calling the `getOnlineUsers` method, your information is not included.</li><li>When calling the `getUserChannels` method, the channels that you subscribe to in silent mode are not included.</li> |

</PlatformWrapper>

#### {enumv.topictype[props.ag_platform]}

<span className="index-desc-topictype">Topic event type.</span>

| Value | Description |
| :----------: | ---------------------------------------------------------- |
| <code>{enumv.topictypesnap[props.ag_platform]}</code> | `1`: The snapshot of the topic when the user joined the channel. |
| <code>{enumv.topictypejoin[props.ag_platform]}</code> | `2`: A remote user joined the channel. |
| <code>{enumv.topictypeleave[props.ag_platform]}</code> | `3`: A remote user left the channel. |
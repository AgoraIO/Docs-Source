<PlatformWrapper platform="electron">
3.  In **preload.js**, update `appID`, `channel` and `token` with your values.

4.  Run the app
    
    Execute the following command in the terminal:
    ```bash
    npm start
    ```
    You see your app opens a window named **Get started with <Vpd k="PRODUCT" />**.

5.  Press **Join** to see the video feed from the web app.

    In the developer console, you see messages displaying the JSON data for the audio frames.

6.  Process raw audio data.

    Update `iaudioFrameObserver` by adding code to a suitable callback that processes `AudioFrame` data to produce the desired effect. Run your <Vpl k="CLIENT" /> again to experience the effects of raw data processing.

</PlatformWrapper>

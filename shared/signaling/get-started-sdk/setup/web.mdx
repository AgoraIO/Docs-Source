<PlatformWrapper platform="web">
import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import CodeBlock from '@theme/CodeBlock';

### Create a project

To create a folder for your project, and an `index.html` file for your <Vpl k="CLIENT" />, execute the following commands in the terminal:

```shell
mkdir HelloWorld
cd HelloWorld
touch index.html
```

### Integrate the SDK

Use either of the following methods to integrate <Vpd k="SDK" /> into your project.

<Tabs>
<TabItem value="tab1" label="Using CDN" default>

1. [Download](/sdks?platform=web) the latest version of <Vpd k="SDK" />.

1. Add the following code to your project to reference the SDK:

    <CodeBlock language="js" showLineNumbers>
    {`<script src="path_to_sdk/agora-rtm.x.y.z.min.js"></script>`}
    </CodeBlock>

    Replace `x.y.z` with the specific SDK version number, such as 2.2.0. To get the latest version number, check the [Release notes](../overview/release-notes).

</TabItem>
<TabItem value="tab2" label="Using npm">

1. Install the SDK using npm

    <CodeBlock language="js" showLineNumbers>
    {`npm install agora-rtm-sdk`}
    </CodeBlock>

1. Import `AgoraRTM` from the SDK

    <CodeBlock language="js" showLineNumbers>
    {`import AgoraRTM from 'agora-rtm-sdk';`}
    </CodeBlock>

</TabItem>
</Tabs>

</PlatformWrapper>
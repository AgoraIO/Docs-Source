<PlatformWrapper platform="android">
        
7.  In your  <Vpl k="CLIENT" / >, Update `appID` in the declarations to the value from  <Vg k="CONSOLE" / > .

2.  Set `token` to an empty string in the declarations.

3.  Set `channelName` to the same string you specified in the web demo app.

4.  Update `serverUrl` to the base address of your token server. For example, `https://<app-name>.herokuapp.com`.

5.  Connect a physical Android device to your development device.

6.  In Android Studio, click **Run app**. A moment later you see the project installed on your device.

    If this is the first time you run your project, grant microphone and camera access to your app.

7.  Click **Join** to connect your Android  <Vpl k="CLIENT" / > to the web demo app.
</PlatformWrapper>

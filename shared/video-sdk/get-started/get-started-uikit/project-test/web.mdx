<PlatformWrapper platform="web">
3. In `<app-name>/src/App.js`, update `appID`, `channel` and `token` with the values for your temporary token.

4. Start the dev server.

   Execute the following commands in the terminal:

   ```bash
   cd <app-name>
   npm start
   ```

   Use the URL displayed in the terminal to open the app in your browser.

5. To connect to a channel, click _Join_.
   If this is the first time you run your app, grant microphone and camera access to your app.
   Now, you can see yourself in the local view and talk to the other user in your app.

</PlatformWrapper>

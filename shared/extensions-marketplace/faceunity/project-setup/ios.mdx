<PlatformWrapper platform="ios">

1. Create a `Resource` folder under your project folder, as follows:
    ```shell
    .
    ├── <ProjectName>
    ├── <ProjectName>.xcodeproj
    └── Resource
    ```
1. Unzip the downloaded package, and save all `.framework` files to the `<ProjectName>` folder. Ensure that you select **Embed & Sign** instead of **Embed**.
1. Save the certificate file `authpack.h` to the `<ProjectName>` folder.
1. Save the model and prop files you need to the `Resource` path. For details of files provided in the resource pack, see [Resource package structure](https://github.com/FaceUnity/FULiveDemo/blob/master/docs/iOS_Nama_Demo_Guide.md#2-file-structure).
1. To import the required header files, add the following statements:

   ``` objective-c
    #import <AgoraRtcKit/AgoraRtcEngineKit.h>
    #import "authpack.h"
   ```

</PlatformWrapper>
<PlatformWrapper platform="web">
        
This migration guide helps you migrate from Signaling `1.x` to Signaling `2.x`.

In December 2023, <Vg k="COMPANY"/> released Signaling `2.x` in response to market and industry needs. Signaling `2.x` brings significant innovation to users in terms of:

* Features coverage, 
* Performance improvement
* Experience optimization.

**Features coverage**: Signaling version `2.x` expands its scope to encompass a broader range of business scenarios through the introduction of functional modules including `Channel`, `Message`, `Topic`, `Presence`, `Storage`, and `Lock`. These additions empower you to transition your attention from fundamental feature implementation to fostering business innovation.

**Performance improvement**: In Signaling, `2.x`, Agora has revamped the backend architecture, enhancing overall performance through optimized network connections. This results in sustained low-latency, higher reliability, increased concurrency, and seamless scalability for real-time network access. With these advancements, businesses can confidently rely on the backend, ensuring both optimal performance and high-quality service without concerns.

**Experience optimization**: Agora has redesigned and simplified the API interface to accommodate the industry's most popular Async/Await programming model. We've optimized all documents, including user guides and API references, and furnished more comprehensive sample codes. These enhancements empower developers with a cost-effective learning experience, enabling swift integration and improved development efficiency when utilizing the SDK for your business needs.

Agora appreciates the unwavering trust and support of its customers in its Signaling products. To facilitate a seamless transition from Signaling `1.x` to Signaling `2.x`, refer to the following document. This resource is designed to expedite the migration process, ensuring that you can swiftly leverage the new features in Signaling `2.x` and start reaping their benefits.

## Activate Signaling

To activate Signaling `2.x`, take the following steps:

1. Log in to [<Vg k="CONSOLE"/>](https://console.agora.io/v2)

1. Create a new Agora project or choose an existing project from the project list.

1. Select the project on the [Project Management](https://console.agora.io/v2/project-management) page and click the pencil icon to configure it.

1. Go to **All features > Signaling > Basic information** and select a data center in the dropdown.

1. Go to **Subscriptions > Signaling** and subscribe to a plan.

1. Copy the  APP ID for your project for use in your code.

## Integrate the SDK

You can integrate the Signaling 2.x SDK into your <Vpl k="CLIENT"/> either through a CDN or by utilizing the NPM package manager. For direct download and package manager links, see [SDKs](../../sdks?platform=web)

* **Local integration using CDN**
    
    Download the Signaling SDK and reference it locally. Replace `x.y.z` in the following example with the specific SDK version number. For example, `2.1.9`.

    ```javascript
    // The following is the 1.x usage for CDN integration.
    <script src="your_path_to_sdk/agora-rtm-sdk.x.y.z.min.js"></script>

    // Here's how to integrate 2.x using the CDN method
    <script src="your_path_to_sdk/agora-rtm.x.y.z.min.js"></script>
    ```
* **Package manager integration using NPM**

    ```javascript
    // Here's how to integrate Signaling 1.x using npm
    npm install agora-rtm-sdk
    // import it in your app.js
    import AgoraRTM from 'agora-rtm-sdk'; 

    // Here's how to integrate Signaling 2.x using npm
    npm install agora-rtm
    // import it in your app.js
    import AgoraRTM from 'agora-rtm'; 
    ```

    <Admonition type="caution" title="Caution">Note that the package names for `2.x` and `1.x` are different. The package name for Signaling `2.x` is `agora-rtm`, while the package name for the `1.x` version is `agora-rtm-sdk`.</Admonition>

## Initialize RTM Client instance


Initializing a Client instance in version `2.x` differs significantly from version `1.x`. The initialization parameters have undergone substantial adjustments, accompanied by the introduction of numerous new features, including end-side encryption, cloudProxy, and more. For details, refer to the [API Reference](../reference/api). Additionally, `2.x` introduces the `try{...} catch{...}` programming mode, enabling you to conveniently capture any exceptions.

Refer to the following Here's some example code from 1.x to initialize the instance

```javascript
// Here's the 1.x sample code for initializing the instance
let options = {
    uid: "", 
    token: ""
}
// Fill in the AppID you got from the Console.
const appID = "<Your app ID>"; // Fill in your App ID.
options.uid = "<Your uid>"; // Fill in your UID
options.token = "<Your token>"; // Fill in your token.
// Initialize the client instance
const rtm = AgoraRTM.createInstance(appID); // Initialize the client instance.

// Here's the 2.x sample code for initializing the instance
const { RTM } = AgoraRTM; 
// Fill in the information you've added from the Console.
const appId = "your_appId"; // Fill in your App ID 
const userId = "your_userId"; // Fill in your User ID  
// Initialize the client instance 
try {
    const rtm = new RTM(appId, userId); // Initialize the client instance.
} catch (status) {
    console.log(status); 
}
```

## Logging in to Signaling

Signaling `2.x` embraces the standard async/await mode to facilitate asynchronous programming. The approach to logging into the service in `2.x` differs from that in `1.x`, as illustrated below

```javascript
// The following is sample code for the 1.x 
await rtm.login(options)

// Here's the sample code for the login service in 2.x.
try {
    const result = await rtm.login();
    console.log(result);
} catch (status) {
    console.log(status);
}
```

## Event notifications

Compared to version `1.x`, Signaling `2.x` overhauls the system event notification model and API interface. It categorizes and consolidates event notification types, to enhance the payload structure for optimal efficiency. Version `2.x` features seven distinct event notification types, outlined as follows.


| Event Types | Description |
|:------------:|-----------------------------------------------------|
| `message` | Message event notification:<br/>Receive all message events in the Message Channel and Topic subscribed by the user. |
| `presence` | Notification of user presence and custom state change events:<br/>Receive all user attendance and custom status change events in the Message Channel subscribed by the user and the Stream Channel joined by the user. |
| `topic`| Receive all Topic change events in the Stream Channel joined by the user. |
| `storage` | Channel Attribute and User Attribute event notifications:<br/>Receive all Channel Metadata events in the Message Channel subscribed by the user and the Stream Channel joined, and receive User Metadata events from the subscribed user. |
| `lock`| Receive all Lock events in the Message Channel subscribed by the user and the Stream Channel joined. |
| `status` | Network Connection Status Change Event Notification:<br/>Receive events for client network connection status changes. |
| `tokenPrivilegeWillExpire` | Receive event notifications when the client token is about to expire. |


For more information about event notifications and payload structures, refer to the [Event Listeners](../reference/api?platform=web#event-listeners) section in the API Reference.

Take listening to message event notifications as an example:

```javascript
// 1.x sample code for listening to channel event notifications.
let channel = rtm.createChannel("demoChannel");
channel.on('ChannelMessage', function (message, memberId) {
    console.log(memberId + ": " + message.text);
})

// 2.x sample code for implementing message event notification listener
rtm.addEventListener("message", event => {
    const channelType = event.channelType; // Which channel type it is, Should be "STREAM" or "MESSAGE" .
    const channelName = event.channelName; // Which channel does this message come from?
    const topic = event.topicName; // Which Topic does this message come from, it is valid when the channelType is "STREAM" . const messageType = event.topicName; // Which channel type it is, Should be "STREAM" or "MESSAGE" .
    const messageType = event.messageType; // Which message type it is, Should be "sting" or "binary" . const customType = event.
    const customType = event.customType; // User defined type
    const publisher = event.publisher; // Message publisher
    const message = event.message; // Message payload
    const pubTime = event.publishTime; // Message publisher timestamp
});
```


From the sample code above, you can see the following significant differences.

- The message event notification of version 1.x is bound to a specific `channel` instance. Users need to use the `createChannel()` method to create a `channel` instance first, and then call `channel` instance. channel` instance first, and then call the `channel.on()` method to bind it with event handler, multiple channels must be bound multiple times. The message event notification of version 2.x is bound to a specific `channel` instance. message event notification of version 2.x is bound to the client instance and is global. Use the `addEventListener()` method to register the callback Use the `addEventListener()` method to register the callback program. You only need to bind it once and it will listen to all subscribed channels or topics.

- The message event notification payload structure of version 1.x contains relatively little information, while the message event notification payload structure of version 2.x contains more information, which can help users better implement their own business.





## Channel messages

In version 1.x, if a user wants to send a channel message, you need to.

- Create a Channel instance

- Join channel

- Send channel messages

There is a disadvantage to this design. You cannot send a message without receiving it. Because sending messages and receiving messages are not decoupled. RTM2.x adopts a new Pub/Sub-based design to decouple sending messages and receiving messages. When sending messages, you only need to Publish to the specified channel without joining this channel. To receive channel messages, you only need to subscribe to the specified channel. To receive channel messages, you only need to subscribe to the specified channel. Channel is sufficient, and the two operations do not affect each other.



```javascript
// 1.x Sample code for sending messages
let channel = rtm.createChannel("demoChannel"); await channel.join(); let channel = rtm.createChannel("demoChannel")
await channel.join();
let payload = "Hello World!"; await channel.sendMessage(channel.sendMessage); let payload = "Hello World!
await channel.sendMessage({ text: payload }).then(() => {
    console.log("Channel message: " + payload + " from " + channel.channelId);
}

// 2.x Sample code for sending a message
const payload = "Hello World!
try {
    const result = await rtm.publish("demoChannel", payload); console.log(result);
    console.log(result);
} catch (status) {
    console.log(status); } catch (status) { console.log(status); }
}

// 2.x Subscribing to channels
try {
    const result = await rtm.subscribe("chat_room"); console.log(result); }
    console.log(result); }
} catch (status) {
    console.log(status); } catch (status) { console.log(status); }
}
```

## Peer-to-peer messaging

The peer-to-peer messaging API in version 1.x is used to send messages to specified users. For example, if you want to send a message to the user whose UserId is "Tony", you can do it as follows.

```javascript
rtm.sendMessageToPeer( 
    { text: 'test peer message' }, // An instance of RtmMessage.
    'PeerId', // The uid of the peer user.
).then(sendResult => {
  if (sendResult.hasPeerReceived) {
    // Your code: the remote user has received the message event.
  } else {
    // Your code: the server has received the message, the peer has not.
  }
}).catch(error => {
   // Your code: peer-to-peer message delivery failed.
});
```


The reason why this interface is designed in version 1.x is to meet the needs of an end-to-end message transmission scenario, that is: user A only wants to send a message to the specified user B, and user B only wants to receive this message and does not want to follow other events. As mentioned earlier, the channel mechanism of version 1.x is a structural design based on the ROOM mode. This design cannot achieve the decoupling of sending messages and receiving messages, so it cannot meet the needs of the previous scenario.

In version 2.x, Signaling redesigned the distribution mode of channel messages, that is, using the Pub/Sub mode to realise the decoupling of sending messages and receiving messages. On this basis, point-to-point messages can be implemented uniformly using channel messages. On this basis, point-to-point messages can be implemented uniformly using channel messages.

- Each user subscribes to a private channel with `inbox_` + `UserID` as the channel name after initialization, which we call **inbox**. Only you can subscribe to this channel, others cannot.

- To send a peer-to-peer message to a user, just send the message to the user's **Inbox**.

```javascript
// Subscribe to your own inbox
const result = await rtm.subscribe("inbox_Tony");
// The content of the message
const payload = {
    type. "PrivateMessage",
    message. "This is a message", sender: "PrivateMessage", message.
    sender. "Tony"
This is a message", sender: "Tony" }; }
try {
    // Send a private message to the user with the UserID "Lily" that only Lily can receive. const result = await rtm.
    const result = await rtm.publish("inbox_Lily", payload); console.log(result); // Send a private message to the user with the UserID "Lily".
    console.log(result);
} catch (status) {
    console.log(status); } catch (status) { console.log(status); }
}
```


The peer-to-peer messaging feature can be implemented according to the above method. Note: To receive messages, you need to implement the `message` event listening function.

## Picture and file messages

For reasons of user data/privacy protection compliance and cost optimisation, RTM no longer directly supports image and file message types after version 1.5.0, and the related interfaces have also been removed. But this does not mean that you cannot use RTM to realise the transmission and distribution of image and file messages. On the contrary, you can build image and file message functions with the help of RTM2.x and third-party object storage services (such as Amazon S3 or Alibaba Cloud OSS). Not only can you get the best factual message transmission experience, but you can also implement more flexible technical solution construction (for example. CDN static resource acceleration or image and text moderation and other business needs). The following shows you how to use RTM2.x and Amazon S3 object storage service to build and send image and file messages.

```javascript
const sendFileMessageAsync = async () => {
    try {
          // Use the document-picker component to pick the file on the device to be uploaded.
          const result = await DocumentPicker.getDocumentAsync({
            type: '*/*', // You can select any type of document.
          }); if ( result.canceled ========== result.getDocumentAsync({ type: '*/*', // any type of document can be selected); }); }

      if ( result.cancelled === false) {
          const file= result.assets[0].file; if(file) { result.canceled === false) { const file= result.assets[0].file; }
          if(file) {
              const uploadParams = {
                    Bucket. 'your-awsbucket', // fill in the name of your bucket on aws S3
                    Key: file.name, // the key of the file on aws S3
                    Body: file, // the file's key on aws S3
                    ContentType: file.type, }; { Bucket: 'your-awsbucket' // fill in your bucket name on aws S3
                    }; // Upload the file to S3.
              // Upload the file to S3
              s3.upload(uploadParams, (err, data) => {
                if (err) {
                  console.error('Error uploading file:', err); } else {
                } else {
                  console.log('File uploaded successfully. S3 URL:', data.Location);
                  // Customise the RTM file message payload structure after the file has been uploaded successfully
                  const imageMessagePayload = {
                        type:'File', // File type, the recipient can parse the message packet structure based on this field
                        bucket:uploadParams.Bucket, // the name of the bucket on your AWS-S3, the receiver needs this field to download the file          
                        key:uploadParams.Key, // the KEY of the file as it is stored on AWS-S3, the recipient needs this field to download the file
                        contentType:uploadParams.ContentType, // the file's type
                        url:data.Location, // file URL address
                        sender:userId // sender USERID               
                        }; // Use RTM to send a file message.
                  // Send the file message load using RTM
                  rtm.publish(channelName,JSON.stringify(imageMessagePayload),{customType:'File'}); }
                  }
                });
          }
      } else {
        console.log('Document pick cancelled'); }
      }
    } catch (err) {
      console.error('Error picking document:', err); }
    }
}
```

> 
> Note: When using AWS-S3 for static file storage, you need to login to the Amazon S3 console and set the correct user permissions and access policies. For details, see. [Amazon Simple Storage Service User Guide](../overview/core-concepts)

## User Attendance and Custom Status

In Signaling 1.x, you can subscribe to or query the online status of multiple users, query the number of users in a channel, or the list of online members of a channel, etc. Signaling 2.x not only retains these capabilities, but also implements upgrades and extensions for these capabilities. In Signaling 2.x, these capabilities are redefined and designed in the Presence module. Presence provides the ability to monitor user online, offline and user status change notifications. Through the Presence function, you can obtain the following information in real time.

- User joins or leaves the specified channel
- Customise temporary user status and its changes
- Query which channels a specified user has joined or subscribed to
- Query which users have joined the specified channel and their temporary status data

You can call the `whoNow` method to query the number of online users of the specified channel, the list of online users, and the temporary status of online users in real time.

```javascript
const options = {
    includeUserId : true,
    includeUserId : true, includeState : true, page : "yourBookMark
    page : "yourBookMark"
}
try{
    const result = await rtm.presence.whoNow( "chat_room", "MESSAGE", options );
    console.log(result);
} catch(status){
    console.log(status); }
}
```


You can call the `whereNow` method to get the list of channels where the specified user is located in real time.

```javascript
try{
    const result = await rtm.presence.whereNow( "Tony" ); console.log(result);
    console.log(result);
} catch(status){
    console.log(status); } catch(status){ console.log(status); }
}
```

In order to meet the user status setting requirements of business scenarios, Signaling 2.x provides temporary user status capabilities. You can customize temporary user status through the `setState` method. Users can add scores, game status, location, mood, continuous wheat status and other customised statuses for themselves.

```javascript
var newState = { "mood": "pumped", "isTyping":false};

try{
    const result = await rtm.Presence.setState("chat_room", "MESSAGE", newState);
    console.log(result);
} catch(status){
    console.log(status); } catch(status){ console.log(status); }
}
```

You can also get the user's online state at any time by using the `getState` method, or remove your own state by using the `removeState` method. The RTM server triggers a `presence` event notification of type `REMOTE_STATE_CHANGED` when the user's temporary state is changed; see: Presence User's Guide for details on how to use this.

In Signaling 2.x, if you want to listen to the real-time notification of user's entry, exit, timeout and temporary status change in a channel, it is more convenient, you only need to implement the following steps:

- Implement Presence event listener

- Enable the withPresence switch when you join the channel.

You can also obtain the user's Presence event listener as shown in the following code:

You can also obtain the user's online status at any time through the `getState` method, or delete your own state through the `removeState` method. After the user's temporary state changes, the RTM server will trigger a `presence` event notification of the `REMOTE_STATE_CHANGED` type. For specific usage, see. Presence User Guide.

In Signaling 2.x , it will be more convenient if you want to monitor real-time notifications of users entering, leaving, disconnecting after timeout, or temporary status changes in the channel. You only need to implement the following steps.

- Implement Presence event listener

- When joining a channel, turn on the withPresence switch

As shown in the following code.



```javascript
// Implement Implement the Presence event listener
rtm.addEventListener("presence", event => {
    const action = event.eventType; // Which action it is ,should be one of 'SNAPSHOT','INTERVAL','JOIN','LEAVE','TIMEOUT,'STATE_CHANGED','OUT_OF_SERVICE'. const channelType = event.switch As shown in the following code: ```javascript // Implement Presence event listener. rtm.addEventListener("presence", event => { event.eventType; })
    const channelType = event.channelType; // Which channel type it is, Should be "STREAM" or "MESSAGE" .
    const channelName = event.channelName; // Which channel does this event come from?
    const publisher = event.publisher; // Who triggers this event
    const states = event.stateChanged; // User state payload
    const interval = event.interval; // Interval payload
    const snapshot = event.snapshot; // Snapshot payload
    // your code here
}
// Enable the withPresence switch when joining a channel.
const options = { withPresence : true }; }
try {
    const result = await rtm.subscribe("chat_room", options); console.log(result); // If you want to add a new channel to the list, you have to add a new channel to the list.
    console.log(result); }
} catch (status) {
    console.log(status); }
}
```
There is a new design for real-time notifications in Signaling 2.x. The presence event notification mode refers to how to notify subscribed users of presence events in the channel. There are two modes: **Real-time notification mode **Real-time notification mode (Announce)** and **Scheduled notification mode (Interval)**. Users can determine the conditions for switching between the two modes through the **Announce Max** parameter in the project settings of the console. The scheduled notification mode can prevent noisy events caused by too many online users in the channel. For details, see. [Event Notification](../overview/core-concepts)

## User metadata and channel metadata

Signaling 2.x retains the full functionality of user metadata and channel metadata, with new capabilities such as versioning and locking, and optimized API interfaces to make them easier to use.

In Signaling 2.x, user attributes and channel attributes are mounted under the `Storage` module. If you want to set a channel attribute, you can do it in the following way:

RTM2.x retains the complete features of user metadata and channel metadata, and adds version control and lock control capabilities on this basis. At the same time, RTM2.x also optimises the API interface form to make the use of functions more convenient. Simple.

In RTM2.x, user metadata and channel metadata are mounted under the `Storage` module. If you want to set channel metadata, you can do it in the following way.

```javascript
const properties = {
    key : "Quantity",
    value : "20"
}; const announcement = { key : "Quantity", value : "20

const announcement = {
    key : "Announcement", value : "Welcome to our shop!
    value : "Welcome to our shop!"
}; const announcement = { key : "Announcement", value : "Welcome to our shop!

const price = {
    key : "T-shirt", value : "100"; const price = { key : "Announcement", value : "Welcome to our shop!
    value : "100"
}; const price = { key : "T-shirt", value : "100"; }


const options = { addTimeStamp : true, addUserId : true }; const data = [properties, announcement, price]; const options = { addTimeStamp : true, addUserId : true }

try {
    const result = await rtm.storage.setChannelMetadata("channel1", "MESSAGE", data, options); console.log(JSON.string(JSON.string))
    console.log(JSON.stringify(result)); }
} catch (status) {
    console.log(JSON.stringify(status)); } catch (status) { console.log(JSON.stringify(status)); }
}
```

How to get, update, delete channel attributes, how to use CAS control, LOCK control, etc. can refer to. [Channel Attributes User Guide](./). The use of user attributes is similar to the use of channel attributes. For details, see. [Channel Attributes User Guide](./).

Events for channel attributes and user attributes are distributed to users through `storage` type event notifications. If you need to listen to such events, you can do it as follows.

```javascript
// Handle storage event
rtm.addEventListener("storage", event => {
    const channelType = event.channelType; // Which channel type it is, Should be "STREAM" or "MESSAGE" .
    const channelName = event.channelName; // Which channel does this event come from?
    const publisher = event.publisher; // Who triggers this event
    const storageType = event.storageType; // which category the event is, should be 'USER', 'CHANNEL'
    const action = event.eventType; // Which action it is ,should be one of "SNAPSHOT", "SET", "REMOVE", "UPDATE" or "NONE"
    const data = event.data; // 'USER_METADATA' or 'CHANNEL_METADATA' payload
    //your code
}); }
// Enable the withStorage switch when joining a channel.
const options = { withMetadata : false }; }
try {
    const result = await rtm.subscribe("chat_room", options); console.log(result); console.log(result); }; // Enable withStorage switch when joining channel.
    console.log(result); }
} catch (status) {
    console.log(status); }
}
```

## Restriction of access area


RTM supports the restricted access area feature to adapt to the laws and regulations of different countries or regions. After turning on the restricted access area feature, no matter which area the user uses your app, the SDK will only access the Agora server in the specified area. RTM 1.x implements access area limitation as follows.



```javascript
rtm.setArea({ areaCodes: ["GLOBAL"], excludedArea: "CHINA" })
```
RTM2.x also supports the above feature, and the usage is exactly the same.

## Other new features

In addition to the feature differences between the above versions, RTM2.x also supports the above feature, and the usage is exactly the same:



In addition to the feature differences between the above versions, Signaling 2.x also adds many other new features that are widely verified and used. Users can choose and use them according to the needs of their own projects. We  believe that the innovative version of Signaling 2.x will definitely help you quickly access the real-time interactive field. The features of Signaling 2.x are listed below, [see API reference for detailed usage](./).



</PlatformWrapper>

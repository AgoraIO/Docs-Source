<PlatformWrapper platform="react-js">

3. In the `video-sdk-samples-reactjs` reference app, open `src/config.json` and update the values of `appId`,
    `channelName`, and `rtcToken` with the values for your temporary token.

1. In Terminal run the following command to start a proxy server:

    ```bash
    node ./utils/proxy.js
    ```

1. In another Terminal instance run the following command:

    ```bash
    yarn dev
    ```

1. Open the project in your browser. The default URL is http://localhost:5173/.

1. In the dropdown, select the Get Started app and test <Vpd k="PRODUCT" />.


</PlatformWrapper>
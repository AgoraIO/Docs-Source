<PlatformWrapper platform="web">

1. **Clone the [<Vpd  k="SDK" /> reference app](https://github.com/AgoraIO/video-sdk-samples-js) repository**.

    Navigate to your `<samples-root>` folder and run the following command:

    ```bash
    git clone https://github.com/AgoraIO/video-sdk-samples-js.git
    ```

1. **Install dependencies**:

    Navigate to `video-sdk-samples-js` root directory and run the following in the terminal:

    ```bash
    pnpm install
    ```

</PlatformWrapper>

<PlatformWrapper platform="electron">
        
4.  In *main.js*, update `appID`, `channel` and `token` with your values.

5.  Start the dev server

    Execute the following command in the terminal:

    ``` bash
     npm start
    ```

    You see your app opens a window named **Get started with <Vpd k="PRODUCT" />**.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
6. Select a user role using the radio buttons and click **Join**.

</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
6.  To connect to a channel, click **Join**.

</ProductWrapper>

7.  Open another instance of your <Vpl k="CLIENT" /> on a test device and update `appID`, `channel` and `token` with your values, then click **Join**.

    You see the local and remote videos on the two devices.
</PlatformWrapper>

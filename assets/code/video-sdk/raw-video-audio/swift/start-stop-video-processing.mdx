<PlatformWrapper platform="macos">
```swift
@objc func zoomBtnClicked(sender: NSButton!) {
    isZoomed = !isZoomed
    zoomBtn.title = "Zoom \(isZoomed ? "Out" : "In")"
}
```
</PlatformWrapper>
<PlatformWrapper platform="ios">
```swift
@objc func zoomBtnClicked(sender: UIButton!) {
    isZoomed = !isZoomed
    zoomBtn.setTitle("Zoom \(isZoomed ? "Out" : "In")", for: .normal)
}
```
</PlatformWrapper>
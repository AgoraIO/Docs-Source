<PlatformWrapper platform="unity">

1. Set the variables and properties in the project code to values suitable for your chosen extension.

2.  Generate a temporary token in Agora Console.

3. In your browser, navigate to the <Link target="_blank" to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link> and update _App ID_, _Channel_, and _Token_ with the values for your temporary token, then click **Join**.

4. In **Unity Editor**, in `Assets/Agora-RTC-Plugin/Agora-Unity-RTC-SDK/Code/JoinChannelVideo.cs`, update `_appID`, `_channelName`, and `_token` with the values for your temporary token.

5.  In **Unity Editor**, click **Play**. A moment later you see the <Vpl k="CLIENT" /> installed on your device.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
6. Select a role using the toggle buttons and click **Join** to start an <Vpd k="PRODUCT" /> session.
</ProductWrapper>

<ProductWrapper product="video-calling">
6. Click **Join** to start a call.
</ProductWrapper>

7. Experience the features of your new extension.

</PlatformWrapper>
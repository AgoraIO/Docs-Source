<PlatformWrapper platform="unreal">

3. Now at your end, considered to be the **Local User**, make changes in **MyUserWidget.h**. Update the `UMyUserWidget` class variables `appId`, `channelName`, and `token` with the values of the generated temporary token. Build and run the code to open **Unreal Editor**.

4. In **Unreal Editor**, navigate to the `Content` folder and double-click `Untitled` in **Content Browser** to load **Level Blueprint**. Click **Play**. A moment later you see the <Vpl k="CLIENT" /> running on your development device.
   If this is the first time you run the project, grant camera and microphone access to your <Vpl k="CLIENT" />.   
   
<ProductWrapper product="interactive-live-streaming">
5. Select a role using the check boxes.

6. Click **Join** to start a call. You can now see yourself on the device screen and talk to the remote user using your <Vpl k="CLIENT" />.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. Click **Join** to start a call. You can now see yourself as well as the remote user on the device screen. Start a video chat with the remote user using this <Vpl k="CLIENT" />.
</ProductWrapper>

</PlatformWrapper>
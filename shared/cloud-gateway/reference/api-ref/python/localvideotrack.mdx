---
title: LocalVideoTrack Class
---


<span className="index-group-localvideotrack" style={{display: 'none'}}>LocalVideoTrack</span>

<span className="index-desc-localvideotrack">`LocalVideoTrack` class. Provides basic methods for the local video track. You can publish one or multiple video tracks through the [`publish_video`](./localuser#publishvideo) method.</span>



<h2 className="anchor index-group-item-localvideotrack index-api-setenabled" id="setenabled">set_enabled</h2>

```python
def set_enabled(self, enable)
```

<span className="index-desc-setenabled">Enables or disables the local video track. After the local video track is enabled, the SDK can capture, process, and encode the local video.</span>

#### Parameters

export const TableHeaderw6h4d4bjye = [
  {
    label: 'Parameter'
  },
  {
    label: 'Description'
  }
];

<Table header={TableHeaderw6h4d4bjye}>
  <Tr>
    <Td>`enable`</Td>
    <Td>
    Enable or disable the local video track:
    - `0`: Disable the local video track.
    - `1`: Enable the local video track.
    </Td>
  </Tr>
</Table>


<h2 className="anchor index-group-item-localvideotrack index-api-setvideoencoderconfiguration" id="setvideoencoderconfiguration">set_video_encoder_configuration</h2>

```python
def set_video_encoder_configuration(self, config)
```

<span className="index-desc-setvideoencoderconfiguration">Sets the video encoding properties.</span>

#### Parameters

| Parameter | Description |
|------|------|
| `config` | Video encoding parameter configuration. See [`VideoEncoderConfig`](./apidatatype#videoencoderconfig). |

#### Returns

- 0: Success.
- < 0: Failure.
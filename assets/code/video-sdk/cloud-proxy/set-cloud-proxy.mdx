<PlatformWrapper platform="android">
    ```java
        int proxyStatus = agoraEngine.setCloudProxy(TRANSPORT_TYPE_UDP_PROXY);
        if (proxyStatus == 0) {
            // Proxy service setup successful
        } else {
            // Proxy service setup failed with error
        }
    ```
</PlatformWrapper>
<PlatformWrapper platform={["ios","macos"]}>
    ```swift
    func setCloudProxy(to proxyType: AgoraCloudProxyType) {
        self.agoraEngine.setCloudProxy(proxyType)
    }
    ```
    <PlatformWrapper platform="ios">
    - <Link to="{{Global.API_REF_IOS_ROOT_RTC_KIT}}/agorartcenginekit/setcloudproxy(_:)">setCloudProxy(_:)</Link>
    </PlatformWrapper>
    <PlatformWrapper platform="macos">
    - <Link to="{{Global.API_REF_MACOS_ROOT_RTC_KIT}}/agorartcenginekit/setcloudproxy(_:)">setCloudProxy(_:)</Link>
    </PlatformWrapper>


</PlatformWrapper>
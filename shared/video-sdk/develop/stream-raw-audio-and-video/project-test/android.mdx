<PlatformWrapper platform="android">

5. **Join a channel** 

6. **Test processing of raw audio data**

    Edit the `iAudioFrameObserver` definition by adding code that processes the raw audio data you receive in the following callbacks:

    -   `onRecordAudioFrame`: Gets the captured audio frame data

    -   `onPlaybackAudioFrame`: Gets the audio frame for playback

<ProductWrapper notAllowed="voice-calling">
7.  **Test processing of raw video data**

    1. Press **Zoom In**. You see that the local video captured by your device camera is cropped and scaled to give a zoom-in effect. The processed video is displayed both locally and remotely. 
    1. Press the button again. Processing of video data stops.

</ProductWrapper>

</PlatformWrapper>

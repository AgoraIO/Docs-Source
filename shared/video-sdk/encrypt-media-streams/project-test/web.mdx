<PlatformWrapper platform="web">
        
3.  In *main.js*, update `appID`, `channel` and `token` with your values.

4.  Start the dev server

    Execute the following command in the terminal:

    ``` terminal
     npm run dev
    ```

    Use the URL displayed in the terminal to open the <Vpl k="CLIENT" /> in your browser.

<ProductWrapper product="interactive-live-streaming">
5. Select a user role using the radio buttons.
</ProductWrapper>

6.  To connect to a channel, click **Join**.

    Grant microphone and camera access to your <Vpl k="CLIENT" />.

2.  Open an instance of your <Vpl k="CLIENT" /> on a test device and update `appID`, `channel` and `token` with your values, then click *JOIN*.

    You see the local and remote videos on the two devices.
</PlatformWrapper>

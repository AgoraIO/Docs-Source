<PlatformWrapper platform="android">
    ```kotlin
    agoraEngine!!.renewToken(rtcToken)
    ```
</PlatformWrapper>

<PlatformWrapper platform="ios, macos">
    ```swift
    self.agoraEngine.renewToken(token)
    ```
</PlatformWrapper>

<PlatformWrapper platform="unity">
    ```csharp
    public void RenewToken()
    {
        if(_token == "")
        {
            Debug.Log("Token was not retrieved");
            return;
        }

        // Update RTC Engine with new token
        agoraEngine.RenewToken(_token);
    }
    ```
    -   <Link target="_blank" to="{{global.API_REF_UNITY_ROOT}}/class_irtcengine.html#api_irtcengine_renewtoken">renewToken</Link>
</PlatformWrapper>
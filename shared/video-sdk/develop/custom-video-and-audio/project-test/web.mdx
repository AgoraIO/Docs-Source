<PlatformWrapper platform="web">

3. In _main.js_, update `appID`, `channel` and `token` with your values.

4. Start the dev server

    Execute the following command in the terminal:

    ```terminal
    npm run dev
    ```
    Use the Url displayed in the terminal to open the <Vpl k="CLIENT" /> in your browser.

<ProductWrapper notAllowed="video-calling">
5. Select a user role using the radio buttons. Press **Join** and grant microphone and camera access to your <Vpl k="CLIENT" />.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. Press **Join** and grant microphone and camera access to your <Vpl k="CLIENT" />.
</ProductWrapper>

6. Test the custom audio and video source. 
    
    You see the web demo app plays the audio and video coming from your <Vpl k="CLIENT" />.

</PlatformWrapper>

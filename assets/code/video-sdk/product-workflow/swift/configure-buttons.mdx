<PlatformWrapper platform="macos">
```swift
volumeSlider = NSSlider()
volumeSlider.frame = CGRect(x: 230, y: 40, width: 240, height: 30)
volumeSlider.minValue = 100
volumeSlider.integerValue = 80
volumeSlider.target = self
volumeSlider.action = #selector(volumeSliderValueChanged)

self.view.addSubview(volumeSlider)

muteSwitch = NSSwitch()
muteSwitch.frame = CGRect(x: 355, y: 10 , width: 40, height: 20)
muteSwitch.target = self
muteSwitch.action =  #selector(volumeSliderValueChanged)
self.view.addSubview(muteSwitch)


screenShareButton  = NSButton()
screenShareButton.frame = CGRect(x: 260 , y: 10, width: 50, height: 20)
screenShareButton.title = "Share"
screenShareButton.target = self
screenShareButton.action = #selector(screenShareAction)
self.view.addSubview(screenShareButton)
```
</PlatformWrapper>
<PlatformWrapper platform="ios">
```swift
volumeSlider = UISlider()
volumeSlider.frame = CGRect(x: 40, y: 550, width: 240, height: 50)
volumeSlider.maximumValue = 100
volumeSlider.value = 80

volumeSlider.addTarget(self, action: #selector(volumeSliderValueChanged), for: .valueChanged)
self.view.addSubview(volumeSlider)

muteSwitch = UISwitch()
muteSwitch.frame = CGRect(x: 300, y: 600, width: 40, height: 50)

muteSwitch.addTarget(self, action: #selector(muteSwitchValueChanged), for: .touchUpInside)
self.view.addSubview(muteSwitch)

```
</PlatformWrapper>
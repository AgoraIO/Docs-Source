<PlatformWrapper platform="macos">
```swift
func updateNetworkStatus(quality: Int) {
    if (quality > 0 && quality < 3) { networkStatus.backgroundColor = NSColor.green }
    else if (quality <= 4) { networkStatus.backgroundColor = NSColor.yellow }
    else if (quality <= 6) { networkStatus.backgroundColor = NSColor.red }
    else { networkStatus.backgroundColor = NSColor.white }
}
```
</PlatformWrapper>
<PlatformWrapper platform="ios">
```swift
func updateNetworkStatus(quality: Int) {
    if (quality > 0 && quality < 3) { networkStatus.backgroundColor = UIColor.green }
    else if (quality <= 4) { networkStatus.backgroundColor = UIColor.yellow }
    else if (quality <= 6) { networkStatus.backgroundColor = UIColor.red }
    else { networkStatus.backgroundColor = UIColor.white }
}
```
</PlatformWrapper>
<PlatformWrapper platform="unity">
    ```csharp
    public void updateNetworkStatus(int quality)
    {  
        if (quality > 0 && quality < 3) 
        {
            networkStatus = "Network Quality: Perfect";
        }
        else if (quality <= 4) 
        {
            networkStatus = "Network Quality: Good";
        }
        else if (quality <= 6) 
        {
            networkStatus = "Network Quality: Poor";
        }
    }
    ```
</PlatformWrapper>

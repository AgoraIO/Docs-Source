The `VideoEncodedFrameObserver` class.

### `OnEncodedVideoFrame`

Occurs when an encoded video frame is received.

```go
OnEncodedVideoFrame func(uid string, imageBuffer []byte, frameInfo *EncodedVideoFrameInfo) bool
```

#### Parameters

| Parameter | Description |
|---------------|----------------------------------------------------------------------|
| `uid` | The user ID of the remote user.             |
| `imageBuffer` | The video frame buffer.             |
| `frameInfo`| The encoded video frame information. See [`EncodedVideoFrameInfo`](#encodedvideoframeinfo) for details.         |

#### Returns

Whether to accept the encoded video frame:

- `true`: Accept.
- `false`: Do not accept.

### OnEncryptionError

```go
OnEncryptionError func(con *RtcConnection, err int);
```

Occurs when encryption errors occur.

<Admonition type="info" title="Since">
v2.2.3.
</Admonition>

#### Parameters

| Parameter | Description |
| --- | --- |
| `con` | [`RtcConnection`](#rtcconnection) object. |
| `err` | Error code. |
<PlatformWrapper platform="web">
3. Set the variables in your <Vpl k="CLIENT" />:
    1.  In your <Vpl k="CLIENT" />, Update the `appID` in the declarations to the value from <Vg k="CONSOLE" /> .

    1.  Set the `token` variable to an empty string in the declarations.

    1.  Update the `serverUrl` in the declarations to the base address of your token server. For example, `https://app-name.herokuapp.com`.

    1.  **If you are developing with <Vg k="UIK" />**: set `channelName` to the same *Channel* you specified in the web demo app.

1.  To start the dev server, execute the following commands in the terminal.

    ``` bash
    npm run dev
    ```

    Use the URL displayed in the terminal to open the app in your browser.

1.  **If you are developing with <Vg k="VSDK" />**: enter the same channel name in the UI text box that you used to connect to the <Vg k="COMPANY" /> web demo.

<ProductWrapper product="interactive-live-streaming">
8.  Select an option . When you select **Host**, your local video is published and played in the <Vpl k="CLIENT" />. When you select **Audience**, your <Vpl k="CLIENT" /> subscribes to and plays to the remote stream. 

9. Press **Join** to connect to the same channel as the web demo.
</ProductWrapper>

<ProductWrapper product = "video-calling">
8. Press **Join** to connect to the same channel as the web demo.
</ProductWrapper>

</PlatformWrapper>

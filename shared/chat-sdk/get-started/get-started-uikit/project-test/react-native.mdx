<PlatformWrapper platform="react-native">

Execute the following inside a console to compile and run the <Vpl k="NAME" /> <Vpd k="NAME"/> <Vg k="UIK" /> example app:

    - For <Vg k="AND" />:
        ```yarn
        cd example && yarn run Android
        ```
    - For <Vg k="IOS" />:
        ```yarn
        cd example && yarn run pods && yarn run iOS
        ```

</PlatformWrapper>
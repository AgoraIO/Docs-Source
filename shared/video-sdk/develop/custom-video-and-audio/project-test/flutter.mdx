<PlatformWrapper platform="flutter">

3. In your IDE, open `/lib/main.dart`, and update `appId`, `channelName` and `token` with the values for your temporary token.

1. Connect a test device to your development device.

1. In your IDE, click *Run app*. A moment later you see the project installed on your device.

    If this is the first time you run the project, grant microphone and camera access to your app.

1. **Test the custom video source**

    1. Select **Host** and press **Join** to join a channel.    

    2. Press **Push Video Frame**. You see the image displayed in the remote video panel of the web demo app.

1. **Test the custom audio source**

    Update code in the basic framework presented above, to do the following:

    1. Modify parameters of the call to `setExternalAudioSource` in `setupVideoSDKEngine` to match your custom audio source.

    1. In `_pushAudioFrame`, add code to read data from your custom audio source into the buffer.

    1. Adjust parameter values to match your custom audio source when creating the `AudioFrame`.

    1. **Join** a channel and press **Push Audio Frame** to start publishing custom audio.

</PlatformWrapper>

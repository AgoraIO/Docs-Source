<PlatformWrapper platform="web">

3.  In **main.js**, update `appID`, `channel` and `token` with your values. Make sure that the `Uid` is set to a non-zero integer.

3.  Update the `apiKey` and `apiSecret` with your values from <Vg k="CONSOLE" />.

3.  Run the app

    Execute the following command in the terminal:
    ```bash
    npm run dev
    ```
    You see your app opens a window named **Get started with <Vpd k="PRODUCT" />**.

<ProductWrapper product="interactive-live-streaming">
6. Select a user role. Press **Join** to connect to the same channel as your web demo.
</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
6. Press **Join** to connect to the same channel as your web demo.
</ProductWrapper>

7. Press **Start speech to text**. You see notifications in **Debug Console** confirming receipt of a builder token and starting of a speech-to-text task. 

7. Speak into the microphone. You see spoken words displayed in your <Vpl k="CLIENT" /> as text.

7. Press **Query speech to text**. You see a message in **Debug Console** informing you of the current status of the task.

7. Press **Stop speech to text**. The speech-to-text task stops and a confirmation message is displayed in **Debug Console**. 

7. Check the storage location you specified in the JSON schema. You see a `WebVTT` file containing speech-to-text data.

</PlatformWrapper>

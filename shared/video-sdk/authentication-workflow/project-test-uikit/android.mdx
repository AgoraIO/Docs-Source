<PlatformWrapper platform="android">
        
3.  In your  <Vpl k="CLIENT" / >, Update `appID` in the declarations to the value from  <Vg k="CONSOLE" / > .

4.  Set `token` to an empty string in the declarations.

5.  Set `channelName` to the same string you specified in the web demo app.

6.  Update `serverUrl` to the base address of your token server. For example, `https://<app-name>.herokuapp.com`.

7.  Connect a physical Android device to your development device.

8.  In Android Studio, click **Run app**. A moment later you see the project installed on your device.

    If this is the first time you run your project, grant microphone and camera access to your app.

9.  Click **Join** to connect your Android  <Vpl k="CLIENT" / > to the web demo app.
</PlatformWrapper>

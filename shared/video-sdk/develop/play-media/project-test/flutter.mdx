<PlatformWrapper platform="flutter">
        
3.  In your IDE, open `/lib/main.dart` and update `appId`, `channelName` and `token` with the values from <Vg k="CONSOLE" />.

4.  Connect a test device to your development device.

5.  In your IDE, click **Run app** or execute the `flutter run` command in the terminal. A moment later, you see the project installed on your device.

    If this is the first time you run the <Vpl k="CLIENT" />, grant camera and microphone permissions.

<ProductWrapper product="interactive-live-streaming">
6. Select **Host** and press **Join** to start a session.
</ProductWrapper>

<ProductWrapper product="video-calling">
6. Click **Join** to start a call.
</ProductWrapper>

7.  Press **Open media file**.

    After a short while, you see a `SnackBar` message confirming that the media file is opened successfully.

1.  Press **Play media file**

    You see the media file played both locally and in the web demo app. The slider indicates the play progress.

1.  Press **Pause playing media**

    Media publishing is paused and the video freezes locally and remotely.

1.  Press **Resume playing media**

    You see that the media file resumes playing.

1.  Wait for the media file to finish playing. When the slider reaches the end, the media player publishing is stopped and camera and microphone publishing is resumed.
</PlatformWrapper>

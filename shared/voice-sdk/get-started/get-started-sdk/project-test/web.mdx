<PlatformWrapper platform="web">

    3.  In *main.js*, update `appID`, `channel` and `token` with your values.

    4.  Start the dev server
        
        Execute the following command in the terminal:

        ```bash
        npm run dev
        ```
        
        Use the Url displayed in the terminal to open the <Vpl k="CLIENT"/> in your browser.

    5.  Press **Join** to connect to a channel.

        If this is the first time you run this <Vpl k="CLIENT"/>, grant microphone access.

    6.  Talk to the web demo app through your <Vpl k="CLIENT"/>.

</PlatformWrapper>
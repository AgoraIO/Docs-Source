import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

<PlatformWrapper platform="react-native">

Take the following steps to test the sample code:

1. Ensure that you have updated the `appId`, `token`, and `channelName` values in your code.

1. Run the app. 

    <Admonition type="caution" title="Caution">Some simulators may not support all the functions of this project. Best practice is to run the project on a physical device.</Admonition>

    <Tabs groupId="platform">
        <TabItem value="tab1" label="Android" default>

        To run the <Vpl k="CLIENT"/> on an Android device:

            1. Turn on the developer options of the Android device and connect the Android device to the computer through a USB cable.

            1. In the project root directory, execute `npx react-native run-android`.

        </TabItem>
        <TabItem value="tab2" label="iOS">
        To run on the <Vpl k="CLIENT"/> on an iOS device:

            1. Open the `ProjectName/ios/ProjectName.xcworkspace` folder using Xcode.

            1. Connect the iOS device to your computer through a USB cable.

            1. In Xcode, click the **Build and Run** button.

        </TabItem>
    </Tabs>

    <Admonition type="info" title="Information">For detailed steps on running the app on a real Android or iOS device, please refer to [Running On Device](https://reactnative.dev/docs/running-on-device).</Admonition>

1. Launch the app and click the **Join channel** button to join a channel.

1. Invite a friend to install and run the <Vpl k="CLIENT"/> on a second device and join the same channel. Once your friend joins the channel, you can see and hear each other.

</PlatformWrapper>
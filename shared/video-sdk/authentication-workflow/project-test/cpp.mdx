import TestHeader from '@docs/shared/common/project-test/index.mdx';

6. **Join a channel**  

    Click **Join** to start a call. Now, you can see yourself on the device screen and talk to the remote user using your <Vpl k="CLIENT"/>.

7. **Experience the token expired and renewed in case of supplied token as balnk (Authenticate Server)** 

    Before 30 second of `expireTime`, a message is prompted that **Token is about to expire**. On clicking it a new token retrieves from server and Agora Engine token is renewed with new token. you get a message **Token Renewed** to validate the same. 

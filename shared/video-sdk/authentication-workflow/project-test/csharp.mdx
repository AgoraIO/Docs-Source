3. In _AgoraImplementation.cs_, set `APP_ID` to the value of your <Vpl k="CLIENT" /> in <Vg k="CONSOLE" />.

4. Set `CHANNEL` and `TOKEN` to empty strings.

5. Update the `serverUrl` in the declarations to the base address of your token server. For example, `\https://agora-token-service-production-92ff.up.railway.app`.

6. Run your <Vpl k="CLIENT" /> `Play`. You see the <Vpl k="CLIENT" /> running on your device.

7. Select a role using the toggle buttons.

8. Set *Channel name* to the name you used in the <Link to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

9. To connect to a channel, click **Join**.
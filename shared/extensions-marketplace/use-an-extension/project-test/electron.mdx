<PlatformWrapper platform="electron">

1.  In **preload.js**, update `appID`, `channel` and `token` with your values.

2.  Run the app
    
    Execute the following command in the terminal:
    ```bash
    npm start
    ```
    You see your app opens a window named **Get started with <Vpd k="PRODUCT" />**.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
3. Select **Host** and press **Join**.

</ProductWrapper>
<ProductWrapper product="video-calling">
3. Press **Join** to connect to the same channel as your web demo.

</ProductWrapper>

4. Press **Enable Extension**. Your <Vpl k = "CLIENT"/> start using the Agora video filter extension.

</PlatformWrapper>

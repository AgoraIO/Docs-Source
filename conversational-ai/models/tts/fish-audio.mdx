---
title: Fish Audio
sidebar_position: 9
platform_selector: false
description: >
 Integrate Fish Audio TTS with the Conversational AI Engine.
last_update:
  date: 2026-06-19
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';
import ParameterList, { Parameter } from '@site/src/components/rest-api/ParameterList';

Fish Audio provides text-to-speech (TTS) with flexible models and high-quality synthetic voices.

<Admonition type="info">
This integration is fully supported for use with <Vpd k="NAME" />. While it has completed functional validation, it is newer to the platform, and additional provider-specific edge cases may be identified as usage scales across a broader range of applications and workloads.
</Admonition>

### Sample configuration

The following examples show how to configure Fish Audio TTS when starting a conversational AI agent.

<Tabs groupId="language">
<TabItem value="python" label="Python SDK">

```python
from agora_agent import Agent
from agora_agent.agentkit.vendors import FishAudioTTS

agent = (
    Agent(name='my-agent')
    .with_stt(...)  # configure your STT vendor
    .with_llm(...)  # configure your LLM vendor
    .with_tts(FishAudioTTS(
        key='your-fishaudio-key',
        reference_id='<voice_model_id>',
        backend='speech-1.5',
    ))
)
```

</TabItem>
<TabItem value="typescript" label="TypeScript SDK">

```typescript
import { Agent, FishAudioTTS } from 'agora-agents';

const agent = new Agent({ name: 'my-agent' })
  .withStt(/* configure your STT vendor */)
  .withLlm(/* configure your LLM vendor */)
  .withTts(new FishAudioTTS({
    key: 'your-fishaudio-key',
    referenceId: '<voice_model_id>',
    backend: 'speech-1.5',
  }));
```

</TabItem>
<TabItem value="go" label="Go SDK">

```go
import "github.com/AgoraIO/agora-agents-go/v2/agentkit/vendors"

agent := agentkit.NewAgent(
    agentkit.WithName("my-agent"),
).WithStt(/* configure your STT vendor */).
  WithLlm(/* configure your LLM vendor */).
  WithTts(
    vendors.NewFishAudioTTS(vendors.FishAudioTTSOptions{
        Key:         "your-fishaudio-key",
        ReferenceID: "<voice_model_id>",
        Backend:     "speech-1.5",
    }),
)
```

</TabItem>
<TabItem value="rest-api" label="REST API" default>

Use the following `tts` configuration in your request:

```json
"tts": {
  "vendor": "fishaudio",
  "params": {
    "api_key": "<fishaudio_key>",
    "reference_id": "<voice_model_id>",
    "backend": "speech-1.5"
  }
}
```

</TabItem>
</Tabs>

<Admonition type="caution">
The parameters listed on this page are validated for use with <Vpd k="NAME" />. Required parameters must be provided as documented. Any additional parameters are passed through directly to the underlying vendor without validation. For a full list of supported options, refer to the [Fish Audio documentation](https://docs.fish.audio/sdk-reference/python/api-reference).
</Admonition>

### Key parameters

<ParameterList title="params" required={true}>
  <Parameter name="api_key" type="string" required={true}>
    The Fish Audio API key used to authenticate requests. You must provide a valid key for the service to function.
  </Parameter>
  <Parameter name="reference_id" type="string" required={true}>
    The identifier of the voice model to use for speech synthesis.
  </Parameter>
  <Parameter name="backend" type="string" required={true}>
    The backend model version to use. For example, `speech-1.5`.
  </Parameter>
</ParameterList>

<PlatformWrapper platform="ios">

3. In Xcode, in `ViewController`, update `appID`, `channelName` and `token` with the values for your temporary token.

1. Run your <Vpl k="CLIENT" /> using either a physical or a simulator iOS device, then wait a few seconds until the installation is complete.

1. If this is the first time you run the project, grant microphone and camera access to your <Vpl k="CLIENT" />.

    If you use an iOS simulator, you see the remote video only. You cannot see the local video stream because of <Link target="_blank" to="https://help.apple.com/simulator/mac/current/#/devb0244142d">Apple simulator hardware restrictions</Link>.

1. **Test the custom video source**

    Add code to the basic framework presented above, to do the following:
    
    1. Convert `Data` to `AgoraVideoFrame` object.

    1. Make `localVideo` render the frames pushed to the SDK.

1. **Test the custom audio source**

    Click **Join** to start a call. You hear audio from the custom source upon joining the call.


</PlatformWrapper>

import TestHeader from '@docs/shared/common/project-test/index.mdx';

<PlatformWrapper platform="react-js">

<TestHeader />

8. **Test <Vpd k="PRODUCT" />**

1. **Start the proxy server**

    In Terminal run the following command:

    ```bash
    node ./utils/proxy.js
    ```

1.  **Run the <Vpl k="NAME" /> reference app**:

    In another Terminal instance run the following command:

    ```sh
    yarn dev
    ```

1. **Open the project in your browser**.

    The default URL is `http://localhost:5173/`.

1. **Test <Vpd k="PRODUCT" />**

    1. In **Choose a product**, select **<Vpd k="NAME" />**.

    1. In **Select a sample code**, select the **Get Started** app and communicate with the web demo.

</PlatformWrapper>
<PlatformWrapper platform="unreal">

Take the following steps to test the sample code:

1. Obtain a temporary token from <Vg k="CONSOLE" />.

1. In `AgoraWidget.h`, update `_appID`, `_channelName`, and `_token` with the app ID, channel name, and temporary token for your project.

1. In the Unreal Editor, click the play button to run your project, then click **Join** to join a channel.

1. Invite a friend to run the demo <Vpl k="CLIENT" /> on a second device. Alternatively, use the [Web demo](https://webdemo.agora.io/basicVideoCall/index.html) to join the same channel. 

    <ProductWrapper notAllowed="voice-calling">
    After your friend joins successfully, you can hear and see each other.
    </ProductWrapper>
    <ProductWrapper product="voice-calling">
    After your friend joins successfully, you can hear each other.
    </ProductWrapper>   
</PlatformWrapper>
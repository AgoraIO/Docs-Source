<PlatformWrapper platform="android">

3. In Android Studio, open `app/java/com.example.<projectname>/MainActivity`, and update `appId`, `channelName` and `token` with the values for your temporary token.

1. Edit the `iAudioFrameObserver` definition to add code that processes the raw audio data you receive in the following callbacks

    * `onRecordAudioFrame`: Gets the captured audio frame data

    * `onPlaybackAudioFrame`: Gets the audio frame for playback

1. Connect a physical Android device to your development device.

1. In Android Studio, click **Run app**. A moment later you see the project installed on your device.

    If this is the first time you run the project, grant microphone access to your app.

1. Press **Join** to hear the processed audio feed from the web app.

</PlatformWrapper>

<PlatformWrapper platform="react-native">
        
3.  In your IDE, open `App.tsx` and update `appId`, `channelName` and `token` with the values from <Vg k="CONSOLE" />.

1. Run your app:

    - *Android*

        1. Enable Developer options on your Android device, and then connect it to your computer using a USB cable.
        1. Run `npx react-native run-android` in the project root directory.

    - *iOS:*

        1. Open the `ProjectName/ios/ProjectName.xcworkspace` folder with Xcode.
        1. Connect your iOS device to your Mac using a USB cable.
        1. Click the **Build and run** button in Xcode.


    If this is the first time you run the project, you need to grant microphone and camera access to your <Vpl k="CLIENT"/>.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
5. Select **Host** and press **Join** to start a session.
</ProductWrapper>

<ProductWrapper product="video-calling">
5. Click **Join** to start a call.
</ProductWrapper>

6.  Press **Open media file**.

    After a short while, you see a message confirming that the media file is opened successfully.

1.  Press **Play media file**

    You see the media file played both locally and in the web demo app. 

1.  Press **Pause playing media**

    Media publishing is paused and the video freezes locally and remotely.

1.  Press **Resume playing media**

    You see that the media file resumes playing.

1.  Wait for the media file to finish playing; media player publishing is stopped and camera and microphone publishing is resumed.
</PlatformWrapper>

<PlatformWrapper platform='android'>

paste before `agoraEngine.enableVideo();`

```java
VirtualBackgroundSource virtualBackgroundSource = new VirtualBackgroundSource();
// Use BACKGROUND_COLOR for a solid color background
// or BACKGROUND_IMG to use a file in PNG or JPG format
virtualBackgroundSource.backgroundSourceType = VirtualBackgroundSource.BACKGROUND_COLOR;
virtualBackgroundSource.color = 0xFFFFFF;
// virtualBackgroundSource.blurDegree = virtualBackgroundSource.BLUR_DEGREE_HIGH;

SegmentationProperty segmentationProperty = new SegmentationProperty();
segmentationProperty.modelType = 1;
segmentationProperty.greenCapacity = 0.5F;

agoraEngine.enableVirtualBackground(true, virtualBackgroundSource, segmentationProperty);
```

</PlatformWrapper>
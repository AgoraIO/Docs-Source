<PlatformWrapper platform="react-native">

3. **Configure the project**:

    In `src/agora-manager/config.json`, Replace:

    * `appId` - with your app ID from [<Vg k="CONSOLE" />](https://console.agora.io/).
    * `channelName` - with the name of a channel you want to join.
    * `rtcToken` - with a valid temporary token for the `channelName`.

1.  **Build the <Vpl k="NAME" /> reference  <Vpl k="CLIENT" />**:
    - **For iOS**:

        Run your project inside an IOS emulator:
       
            ```bash
            yarn run ios
            ```
    - **For Android**:
      
        Run your project inside an Android emulator:
         
        ```bash
        yarn run android
        ```
        Ensure that you have configured a video device for your Android virtual machine.

1. **Run the  <Vpl k="NAME"/> reference <Vpl k="CLIENT" />**

    From the main screen of the <Vpl k="CLIENT" />, choose **<Vpd k="NAME" />** from the dropdown and then select
    **SDK Quickstart**.

1. **Join a Channel**:

    <ProductWrapper product="{['interactive-live-streaming', 'broadcast-streaming']}">
    Select an option and click **Join** to start a session.
    - When you join as a **Host**, the local video is published and played in the <Vpl k="CLIENT"/>.
    - When you join as **Audience**, the remote stream is subscribed and played.
    </ProductWrapper>

    <ProductWrapper product="video-calling">
    Click **Join** to start a call. Now, you can see yourself on the device screen and talk to the remote user using your <Vpl k="CLIENT"/>.
    </ProductWrapper>

</PlatformWrapper>

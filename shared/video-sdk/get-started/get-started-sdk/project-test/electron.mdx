<PlatformWrapper platform="electron">

2.  In **preload.js**, update `appID`, `channel` and `token` with your values.

3.  Run the app
    
    Execute the following command in the terminal:
    ```bash
    npm start
    ```
    You see your app opens a window named **Get started with <Vpd k="PRODUCT" />**.

<ProductWrapper product="interactive-live-streaming">
4. Select a user role using the radio buttons.

5.  To connect to a channel, click **Join**.

6.  Now, you can see yourself in the local view and talk to the other user in your <Vpl k="CLIENT"/>.
</ProductWrapper>
<ProductWrapper product="video-calling">
4.  To connect to a channel, click **Join**.

5.  Now, you can see yourself in the local view and talk to the other user in your <Vpl k="CLIENT"/>.
</ProductWrapper>
</PlatformWrapper>

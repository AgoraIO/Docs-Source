<PlatformWrapper platform="flutter">

3. In `/lib/main.dart`, update `appId`, `channelName` and `token` with the values for your temporary token.

4. Connect a test device to your development device.

5. In your IDE, click *Run app* or execute the `flutter run` command in the terminal. A moment later you see the project installed on your device.
   If this is the first time you run the project, grant microphone and camera access to your <Vpl k="CLIENT" />.

<ProductWrapper notAllowed="video-calling">
6. Select an option and click **Join** to start a session. 

    You see that your <Vpl k="CLIENT" /> connects to the specified region <Vg k="AGORA_BACKEND" /> and opens a channel.
</ProductWrapper>

<ProductWrapper product="video-calling">
6. Click **Join** to start a call.

    You see yourself and the remote user on the device screen. It means your <Vpl k="CLIENT" /> has made a connection with the <Vg k="AGORA_BACKEND" /> in the specified region.
</ProductWrapper>

7. Try excluding a geographical region.

    In `/lib/main.dart`, locate the `setupVoiceSDKEngine` method and replace the `agoraEngine.initialize` call with the following:

    ```dart
    // Exclude India from Global regions for connection
    await agoraEngine.initialize(RtcEngineContext(
        areaCode: (AreaCode.areaCodeGlob.value() ^ AreaCode.areaCodeIn.value()),
        appId: appId));
    ```
</PlatformWrapper>

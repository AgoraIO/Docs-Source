<PlatformWrapper platform="flutter">

1. Set the variables and properties in the framework code to values suitable for your chosen extension.

1.  In your IDE, open `/lib/main.dart` and update `appId`, `channelName` and `token` with the values from <Vg k="CONSOLE" />.

1.  Connect a test device to your development device.

1.  In your IDE, click **Run app** or execute the `flutter run` command in the terminal. A moment later, you see the project installed on your device.

    If this is the first time you run the <Vpl k="CLIENT" />, grant camera and microphone permissions.

1. Join the same channel from another test device or the <Link target="_blank" to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

1. Experience the features of your new extension.

</PlatformWrapper>

<PlatformWrapper platform="unity">

3. In **Unity Editor**, in `Assets/Agora-Unity-<Vg k="VSDK" />-SDK/Code/JoinChannelVideo.cs`, update `_appID`, `_channelName` and `_token` with the values for your temporary token.

4. In Unity Editor, click `Play`. A moment later you see the <Vpl k= "CLIENT"/> running on your development device.

5. **Test the custom audio source**

    Press **Join**. You hear the audio file streamed to the web demo app. 

    To use this code for streaming data from your particular custom audio source, modify the `rawBuffer` in the `AudioFrame` to read the audio data from your source, instead of a raw audio file.

</PlatformWrapper>

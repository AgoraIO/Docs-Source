<PlatformWrapper platform="ios">

3. Set the variables in your <Vpl k="CLIENT" />:
    In Xcode, update the following declarations in `ViewController`:

    -   `appID` - the value from <Vg k="CONSOLE" />.

    -   `serverUrl` - the base address of your token server. For example, `https://app-name.herokuapp.com`.

    -   **If you are developing with <Vg k="UIK" />**: set `channelName` to the same *Channel* you specified in the web demo app.

1.  Connect a physical iOS device to your development device.

1.  In Xcode, click Run, then wait a few seconds until the installation is complete.

    If this is the first time you run your project, grant microphone and camera access to your app.

1.  **If you are developing with <Vg k="VSDK" />**: enter the same channel name in the UI text box that you used to connect to the <Vg k="COMPANY" /> web demo.

<ProductWrapper product="interactive-live-streaming">
7. Choose **Broadcaster** to host an event, choose **Audience** to view and listen to a host.

8.  Click **Join** and see the video feed from the web app.
</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
7.  Click **Join** and see the video feed from the web app.
</ProductWrapper>

</PlatformWrapper>

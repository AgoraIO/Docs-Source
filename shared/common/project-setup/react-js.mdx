<PlatformWrapper platform="react-js">

<ProductWrapper notAllowed="signaling" >
1. **Clone the [<Vpd  k="SDK" /> reference app](https://github.com/AgoraIO/video-sdk-samples-reactjs) to your
    development environment**:

    ```bash
    git clone https://github.com/AgoraIO/video-sdk-samples-reactjs
    ```

1. **Install the dependencies**:

    In Terminal, navigate to `video-sdk-samples-reactjs`, and execute the following command.

    ```bash
    npm install
    ```
    <Vpd  k="SDK" /> is installed automatically. However, you can also [Install <Vpd  k="SDK" /> manually](../reference/downloads#through-the-agora-website).

</ProductWrapper>

</PlatformWrapper>
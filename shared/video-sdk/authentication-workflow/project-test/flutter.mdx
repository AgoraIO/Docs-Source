<PlatformWrapper platform="flutter">
3. Set the variables in your <Vpl k="CLIENT" />:
    1. In your project, Update `appId` in the declarations to the value from <Vg k="CONSOLE" />.

    1.  Update the `serverUrl` in the declarations to the base address of your token server, for example, `https://app-name.herokuapp.com`.

    1. Set `token` to an empty string in the declarations.

    1.  **If you are developing with <Vg k="UIK" />**: set `channelName` to the same *Channel* you specified in the web demo app.

1. Connect a physical device to your development device.

1. In your IDE, click **Run app** or launch your app from the terminal using `flutter run lib/main.dart`. A moment later you see the project installed on your device.

    If this is the first time you run your project, grant microphone and camera access to your app.

1.  **If you are developing with <Vg k="VSDK" />**: enter the same channel name in the UI text box that you used to connect to the <Vg k="COMPANY" /> web demo.

1. Click **Join** to connect your Android <Vpl k="CLIENT" /> to the web demo app.

</PlatformWrapper>

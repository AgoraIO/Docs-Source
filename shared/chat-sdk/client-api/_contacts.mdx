import * as data from '@site/data/variables';
import Understand from '@docs/shared/chat-sdk/client-api/contacts/understand/index.mdx';
import ProjectImplement from '@docs/shared/chat-sdk/client-api/contacts/project-implementation/index.mdx';

After logging in to Chat, users can start adding contacts and chatting with each other. They can also manage these contacts, for example, by adding, retrieving and removing contacts. They can also add the specified user to the blocklist to stop receiving messages from that user.

This page shows how to use the Chat SDK to implement contact management.

## Understand the tech

<Understand />

## Prerequisites

Before proceeding, ensure that you meet the following requirements:

- You have integrated the Chat SDK, initialized the SDK and implemented the functionality of registering accounts and login. For details, see <Link to="/agora-chat/get-started/get-started-sdk">Chat <Vg k="GET_STARTED"/></Link>.
- You understand the API call frequency limits as described in [Limitations](../reference/limitations).

## Implementation

This section shows how to manage contacts with the methods provided by the Chat SDK.

<ProjectImplement />
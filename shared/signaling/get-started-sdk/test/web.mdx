<PlatformWrapper platform="web">

2. In `src/signaling_manager/config.json`, replace `uid`, `appId`, `channelName`, and `token` with the app ID, channel
  name, and authentication token for `uid`.

1. Open a command prompt in the project folder, and run the following command:

    ``` bash
    npm run build
    npm run start:dev
    ```

1. Open the project at `http://localhost:9000/sdk_quickstart/`.

1. Login to <Vg k="MESS" />, then send and receive messages.


</PlatformWrapper>
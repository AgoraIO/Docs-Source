<PlatformWrapper platform="macos">

<ProductWrapper notAllowed="signaling">

1. **Clone the <Vpd  k="SDK" /> reference app to `<samples-root>` on your development environment**:

  Navigate to your `<samples-root>` folder and run the following command:
  
    ```bash
    git clone https://github.com/AgoraIO/video-sdk-samples-macos.git
    ```
  

1. Open the sample project in Xcode.
  
    Select **File** > **Open...** then navigate to `<samples-root>/video-sdk-samples-macos/Docs-Examples.xcodeproj` and click **Open**. Xcode loads the project.
  
</ProductWrapper>

<ProductWrapper product="signaling">

1. **Clone the repository**

    To clone the repository to your local machine, open Terminal and navigate to the directory where you want to clone the repository. Then, use the following command:
    
    ```sh
    git clone https://github.com/AgoraIO/signaling-sdk-samples-ios.git
    ```

1. **Open the project**

   Navigate to `Example-App`, and open `Example-App.xcodeproj`.
   
   All dependencies are automatically installed through the Swift Package Manager.

1. **Update Signing**

    In the Xcode project, open the target, add your development team, and make the bundle identifier unique under **Signing & Capabilities**.

</ProductWrapper>
</PlatformWrapper>
<PlatformWrapper platform="python">

1. Install the build tools for compiling the SDK.

    ```
    sudo apt install build-essential python3-dev
    ```

1. To implement structured, asynchronous event handling in Python, install the `pyee` library.

    ```
    pip3 install pyee
    ```

1. Install the <Vg k="COMPANY" /> server side <Vpl k="NAME" /> SDK.

    ```
    pip3 install agora-python-server-sdk
    ```

    <Admonition type="info" title="Note">
    The Python SDK is a server side SDK.
    </Admonition>


</PlatformWrapper>
<PlatformWrapper platform="web">
        
6.  In your  <Vpl k="CLIENT" / >, Update the `appID` in the declarations to the value from Agora Console.

2.  Set the `token` variable to an empty string in the declarations.

3.  Set `channel` to the same string you specified in the web demo app.

4.  Update `tokenUrl` to the base URL of your token server. For example, `https://<app-name>.herokuapp.com`.

5.  Start the dev server.

    In the terminal, execute the following commands.

    ``` terminal
    cd <app-name>
    npm start
    ```

    Use the URL displayed in the terminal to open the app in your browser.
</PlatformWrapper>

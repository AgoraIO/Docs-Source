---
title: OpenAI
sidebar_position: 2
platform_selector: false
description: >
  Integrate an OpenAI LLM into Conversational AI Engine.
---

import Tabs from '@theme/Tabs';
import TabItem from '@theme/TabItem';

OpenAI provides frontier models ranging from lightweight to high-capability.

### Sample configuration

The following examples show a starting `llm` parameter configuration you can use when you [Start a conversational AI agent](/conversational-ai/rest-api/agent/join).

<Tabs>
<TabItem value="managed" label="Managed mode">

When using managed mode, `api_key` is not required. For more information, see [Use managed mode](/conversational-ai/develop/managed-mode).

```json
"llm": {
  "credential_mode": "managed",
  "vendor": "openai",
  "style": "openai",
  "url": "https://api.openai.com/v1/chat/completions",
  "params": {
    "model": "gpt-4.1-mini"
  },
  "system_messages": [
    {
      "role": "system",
      "content": "You are a helpful chatbot."
    }
  ],
  "max_history": 32,
  "greeting_message": "Hello, how can I assist you?",
  "failure_message": "Please hold on a second."
}
```

</TabItem>
<TabItem value="byok" label="Use your own API key (BYOK)">

```json
"llm": {
  "url": "https://api.openai.com/v1/chat/completions",
  "api_key": "<your_llm_key>",
  "system_messages": [
    {
      "role": "system",
      "content": "You are a helpful chatbot."
    }
  ],
  "max_history": 32,
  "greeting_message": "Hello, how can I assist you?",
  "failure_message": "Please hold on a second.",
  "params": {
    "model": "gpt-4o-mini"
  }
}
```

</TabItem>
</Tabs>

### Key parameters

When using managed mode, `api_key` is not required. For more information, see [Use managed mode](/conversational-ai/develop/managed-mode).

<ParameterList title="llm">
  <Parameter name="api_key" type="string">
    Create or manage your API key from [OpenAI organization settings](https://platform.openai.com/settings/organization/api-keys). Required when using your own API key (BYOK).
  </Parameter>
  <Parameter name="url" type="string">
    Use the completions endpoint.
  </Parameter>
  <Parameter name="params" type="object">
    <Parameter name="model" type="string">
      Refer to [OpenAI models](https://platform.openai.com/docs/models) for available models.
    </Parameter>
  </Parameter>
</ParameterList>

For complete `llm` configuration options, see [LLM parameters](/conversational-ai/rest-api/agent/join#properties-llm). For OpenAI-specific options and model capabilities, see the [OpenAI API documentation](https://platform.openai.com/docs/api-reference/chat).
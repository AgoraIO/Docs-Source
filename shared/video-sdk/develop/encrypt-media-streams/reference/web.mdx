<PlatformWrapper platform="web">

### Development considerations

The media stream encryption feature is supported in both communication and live broadcasting use-cases. However, in the live broadcasting use-case, <Vg k="COMPANY" /> does not support pushing the encrypted media stream to CDN.

### API reference

- <Link to="{{Global.API_REF_WEB_ROOT}}/interfaces/iagorartcclient.html#setencryptionconfig">`setEncryptionConfig`</Link>
- <Link to="{{Global.API_REF_WEB_ROOT}}/globals.html#encryptionmode">`EncryptionMode`</Link>

</PlatformWrapper>

import TestHeader from '@docs/shared/common/project-test/index.mdx';

<PlatformWrapper platform="react-js">

<TestHeader />

8. **Choose this sample in the reference app**

    In **Choose a sample code**, select **Secure authentication with tokens**.

1. **Join a channel**
    1. Specify a channel name in the input field.
    <ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
    2. Click **Join** to start a session. When you select **Host**, the local video is published and played in the <Vpl k="CLIENT" />. When you select **Audience**, the remote stream is subscribed and played.
    </ProductWrapper>

    <ProductWrapper notAllowed="interactive-live-streaming">
    2. Press **Join** to connect to the same channel as your web demo.
    </ProductWrapper>

</PlatformWrapper>
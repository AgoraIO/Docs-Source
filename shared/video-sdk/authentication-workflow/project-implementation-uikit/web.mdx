<PlatformWrapper platform="web">

### Authentication using <Vg k="UIK" />

1.  **Set the URL of the server your <Vpl k="CLIENT" /> retrieves tokens from**

    In `<project_name>/src/App.js`, locate the `rtcProps` declarion and replace `token : ''` with the following code:

    ```javascript
    // The base URL to your token server. For example, https://agora-token-service-production-92ff.up.railway.app"
    tokenUrl : ''
    ```
    Make sure you specify the token-server URL in exactly the same format as shown in the example.
    
<Vg k="COMPANY" /> <Vg k="UIK" /> automatically fetches a token from the token server at `tokenUrl`.

</PlatformWrapper>

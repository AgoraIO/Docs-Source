<PlatformWrapper platform="web">

2.  In **main.js**, update `appID`, `channel` and `token` with your values.

3.  Start the dev server

  Execute the following command in the terminal:

  ```bash
  npm run dev
  ```

  Use the URL displayed in the terminal to open the <Vpl k="CLIENT"/> in your browser.

<ProductWrapper product="interactive-live-streaming">
4. Select a user role using the radio buttons.
</ProductWrapper>

5.  To connect to a channel, click **Join**.

6.  Grant microphone and camera access to your <Vpl k="CLIENT"/>.

7.  Now, you can see yourself in the local view and talk to the other user in your <Vpl k="CLIENT"/>.
</PlatformWrapper>

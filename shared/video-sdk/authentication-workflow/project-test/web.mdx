<PlatformWrapper platform="web">

    3.  In your <Vpl k="CLIENT" />, Update the `appID` in the declarations to the value from <Vg k="CONSOLE" /> .

    2.  Set the `token` variable to an empty string in the declarations.

    3.  If you are modifying the **Get Started with <Vpd k="NAME"/> using <Vpd k="SDK"/>** project, do the follwing:

        1.  Update the `serverUrl` in the declarations to the base address of your token server. For example, `https://app-name.herokuapp.com`.

        2.  To start the dev server, execute the following commands in the terminal.

            ``` terminal
            npm run dev
            ```

        Use the URL displayed in the terminal to open the app in your browser.

        3.  Update the channel name, in the text box at the top, to the same string you used on the demo page and then click **JOIN**.

    <ProductWrapper product="interactive-live-streaming">

    8.  Select an option . When you select **Broadcaster**, your local video is published and played in the <Vpl k="CLIENT" />. When you select **Audience**, your <Vpl k="CLIENT" /> subscribes to and plays to the remote stream.

    </ProductWrapper>


    6.  If you are modifying the **Get Started with <Vpd k="NAME"/> using <Vg k="UIK" />** project, do the following:

        1.  Set the `channel` to the same string you specified in the web demo app.

        3.  Update the `tokenUrl` in the declarations to the base address of your token server. For example, `https://app-name.herokuapp.com`.

        4.  To start the dev server, execute the following commands in the terminal.

            ``` terminal
            cd <app-name>
            npm start
            ```

            Use the URL displayed in the terminal to open the app in your browser.
</PlatformWrapper>

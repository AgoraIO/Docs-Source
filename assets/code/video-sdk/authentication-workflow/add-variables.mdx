
<PlatformWrapper platform="macos">
    ```swift
    // Update with the temporary token generated in Agora Console.
    var token = ""

    // Update with the channel name you used to generate the token in Agora Console.
    var channelName = ""

    // Store the name of the channel to join
    var channelTextField: NSTextField!

    // Add the base URL to your token server. For example, https://agora-token-service-production-92ff.up.railway.app"
    var serverUrl = ""
    // The ID of the app user
    var userID = 0
    // Expire time in Seconds.
    var tokenExpireTime = 40
    ```
</PlatformWrapper>
<PlatformWrapper platform="ios">
    ```swift
    // Update with the temporary token generated in Agora Console.
    var token = ""

    // Update with the channel name you used to generate the token in Agora Console.
    var channelName = ""

    // Store the name of the channel to join
    var channelTextField: UITextField!

    // Add the base URL to your token server. For example, https://agora-token-service-production-92ff.up.railway.app"
    var serverUrl = ""
    // The ID of the app user
    var userID = 0
    // Expire time in Seconds.
    var tokenExpireTime = 40
    ```
</PlatformWrapper>
<PlatformWrapper platform="web">
    ```javascript
    let role = "publisher"; // set the role to "publisher" or "subscriber" as appropriate
    ```
</PlatformWrapper>
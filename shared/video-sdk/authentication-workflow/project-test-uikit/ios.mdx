<PlatformWrapper platform="ios">
        
5.  In Xcode, update the following declarations in `ViewController`:

    -   `appID` - the value from Agora Console.

    -   `serverUrl` - the base address of your token server. For example, `https://app-name.herokuapp.com`.

2.  Connect a physical iOS device to your development device.

3.  In Xcode, click Run, then wait a few seconds until the installation is complete.

    If this is the first time you run your project, grant microphone and camera access to your app.

6.  Update the channel name in the text box at the top to the same string you used on the web demo page.

<ProductWrapper product="video-calling">
8. Choose **Broadcast** to host an event, choose **Audience** to view and listen to the host.
</ProductWrapper>

9.  Click **Join** and see the video feed from the web app. To leave the app, click **Leave**.
</PlatformWrapper>

<PlatformWrapper platform="macos">

There are no libraries available to generate authentication tokens using Swift. An easy workaround for this is to implement a token generator using python libraries and host it on a server. Your <Vpl k="CLIENT" /> can make an HTTP get request to this server and fetch the token.

This section shows you how to implement a token generator using python.

</PlatformWrapper>

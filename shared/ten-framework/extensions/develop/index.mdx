import Cpp from './cpp.mdx'
import Go from './go.mdx'
import Python from './python.mdx'

<PlatformWrapper notAllowed="python">
This page guides you through developing a <Vg k="AI_FRAMEWORK" /> extension using <Vpl k="NAME" />. It covers:

- Creating an extension development project with `arpm`.
- Implementing extension functionality such as sending and receiving messages.
<PlatformWrapper platform="go">
- Configuring `cgo`, if necessary.
</PlatformWrapper>
- Writing unit tests and debugging code.
- Deploying the extension locally within an app for integration testing.
- Debugging the extension code in an app.

<Admonition>
Unless otherwise noted, the commands and code in this tutorial have been tested in a Linux environment. The steps for other platforms are generally similar.
</Admonition>

## Prerequisites

To set up your environment for developing a <Vg k="AI_FRAMEWORK" /> extension:

* Download the latest version of `arpm` and configure the `PATH`. To check you configuration, run the following command:

    ```bash
    arpm -h
    ```

    You see usage information for `arpm`.

</PlatformWrapper>

<Cpp />
<Go />
<Python />
<PlatformWrapper platform="web">
3. In _main.js_, update `appID`, `channel` and `token` with your values.
4. Start the dev server.

   Execute the following commands in the terminal:

   ```bash
   npm run dev
   ```

   Use the URL displayed in the terminal to open the app in your browser. 
<ProductWrapper notAllowed="video-calling">
4. Select a user role using the radio buttons.
</ProductWrapper>
5. To connect to a channel, click *Join*.
6. Grant microphone and camera access to your {props.CLIENT}.
7. Now, you can see yourself in the local view and talk to the other user in your {props.CLIENT}.
</PlatformWrapper>

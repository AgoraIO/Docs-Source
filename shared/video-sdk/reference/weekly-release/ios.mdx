<PlatformWrapper platform="ios">

<Admonition type="caution" title="Note">
Starting from v4.5.0, both <Vpd k="SDK" /> and <Vg k="SIG" /> SDK (v2.2.0 and above) include the `aosl.xcframework` library. If you manually integrate <Vpd k="SDK" /> via CDN and also use Signaling SDK, delete the earlier version of the `aosl.xcframework` library to avoid conflicts. 
</Admonition>

### v4.5.3

Released on April 7, 2026.

<Admonition type="info">
The `aosl.xcframework` library version in RTC SDK v4.5.3 is 1.3.7.
</Admonition>

#### New features

This release includes the following new features.

- **Audio buffer delay query in Media Player**

  To resolve audio sync issues between lead vocals and accompaniment in karaoke scenarios, this release adds the `getAudioBufferDelay` method to the Media Player module to retrieve millisecond-level audio buffer delay during media playback.

- **Multipath network transmission**

  This release introduces multipath transmission for devices with multiple network interfaces (for example, 5G, Wi-Fi, and LAN), effectively reducing or eliminating quality degradation under poor network conditions. This feature is suitable for stable real-time audio and video communication in complex environments such as vehicles, IoT, trains, and highways.

  Enable multipath transmission by setting `enableMultipath` to `true` in `AgoraChannelMediaOptions`. Two transmission modes are supported:

  - **Dynamic mode (default)**: Dynamically selects the optimal path based on network conditions. Suitable for traffic-sensitive, high-experience scenarios such as meetings and education. You can specify a preferred network type using `preferMultipathType`.
  - **Full redundancy mode**: Transmits data simultaneously over all available paths. Suitable for traffic-insensitive, ultra-high-experience scenarios such as outdoor broadcasting and remote control. This mode incurs additional charges; contact technical support to enable it.

  When enabled, the SDK reports real-time transmission statistics for each path through the `rtcEngine:multiPathStats:` callback, including traffic usage for monitoring and optimization.

- **Beauty filter refactoring**

  The beauty filter module has been completely refactored and upgraded, expanding features, improving effects and performance, and resolving issues such as complex parameter configuration, high integration costs, and network-dependent beauty resources. Key improvements include:

  - **New beauty features**: Teeth whitening, nasolabial fold removal, dark circle removal, and eye brightening; new makeup effects including eye bags and facial highlights; face shaping features increased from 11 to 29; new sticker functionality.
  - **Simplified APIs**: Basic beauty, face shaping, makeup, and filters are consolidated into three core nodes — `beauty`, `styleMakeup`, and `filter` — plus the new `sticker` node, represented by cases in `AgoraVideoEffectNodeId`. These are managed uniformly via `AgoraVideoEffectObject` for lifecycle and parameter control, reducing performance overhead from frequent API calls.
  - **Ready-to-use presets**: Multiple preset style templates (for example, Natural Beauty, Campus Makeup, and Cool White Filter) simplify integration without requiring manual tuning of individual parameters.
  - **Dynamic parameter control**: Supports real-time reading and modification of fine-grained beauty parameters via key-value pairs, applying presets, saving custom configurations, and resetting to defaults.
  - **Local beauty resources**: All beauty resources (effects, filters, and makeup) are packaged as local bundle files, eliminating failures caused by network download issues.

- **Asynchronous engine destruction**

  This release adds a new `destroy:` method that supports both synchronous and asynchronous engine destruction via configuration parameters. When destroyed asynchronously, the SDK triggers the `engineReleasedBlock` callback.

- **Additional features**

  - Local snapshot upload now supports setting the video observation position via the new `position` member in `AgoraContentInspectModule`, allowing snapshots of raw video data or video streams before and after effect processing.
  - Added `preloadEffectEx:soundId:filePath:startPos:` and `preloadEffectEx:soundId:filePath:` to preload audio effect files (local or online) to a specified channel for fast playback in multi-channel scenarios.
  - Added `playEffectEx:soundId:filePath:loopCount:pitch:pan:gain:publish:startPos:` to play audio effect files in a specified channel, with support for loop count, pitch, spatial position, volume, publishing to the channel, and starting position.
  - Added error codes `AgoraErrorCodePcmSendFormat (200)` and `AgoraErrorCodePcmSendBufferOverflow (201)` to report PCM data transmission errors.

#### Improvements

This release includes the following enhancements.

- **Device compatibility**

  This release optimizes iOS capture logic, improves stability across various device scenarios, and adapts video codec strategies based on device capabilities.

- **Usability improvements**

  - Optimizes camera capture restart timing during app foreground and background switching, reducing switching latency and issues such as capture interruption and preview black screen.
  - Improves SDK stability when using the system keyboard voice input feature on iPad.

- **Experience improvements**

  - Improves capture quality in low-light environments, reducing noise and restoring dark scene details for better video performance.
  - Optimizes video capture delay estimation for better audio-video sync and lower latency.
  - Improves hardware codec capability query to reduce engine initialization time and enhance compatibility.
  - Works around an iOS system bug to improve audio and video interaction experience in certain scenarios.

- **Package size**

  - Audio SDK package size reduced by 11.3% (compressed arm64 single architecture).
  - Up to 25.47% total size reduction when stripping the ffmpeg library.

- **Performance**

  Reduces CPU usage for video host and 1v1 audio call scenarios.

- **End-to-end latency**

  - Video end-to-end latency reduced by 20–30 ms in 1v1 calls.
  - Video end-to-end latency reduced by 100–600 ms (20%–45%) under restricted bandwidth.
  - Audio end-to-end latency reduced by 50–200 ms in interactive live streaming under jitter-buffered poor networks.

#### Compatibility changes

This release introduces the following compatibility changes.

- **ffmpeg library renamed**: The `Agoraffmpeg.xcframework` library is renamed to `AgoraffmpegExtension.xcframework`. Additionally, the `AgoraffmpegExtension.xcframework` library in the iOS Audio SDK supports stripping from this version. Contact technical support if you need this feature.

  <details>
  <summary>Behavior changes when stripping the ffmpeg library</summary>

  If you integrate the Audio SDK and strip the `AgoraffmpegExtension.xcframework` library, the following behavior changes apply:
  - When playing media files, the app uses the device's native player. Supported formats depend on the device's capabilities.
  - The SDK's built-in media player (`IMediaPlayer`) only supports the following APIs:

    - Initialization
      - `createMediaPlayer`
      - `destroyMediaPlayer`
    - Opening media resources
      - `open`
      - `openWithMediaSource`
    - Playback control
      - `selectAudioTrack`
      - `setAudioDualMonoMode`
      - `setAudioPitch`
      - `setLoopCount`
      - `setPlaybackSpeed`
    - Player control
      - `adjustPlayoutVolume`
      - `adjustPublishSignalVolume`
      - `mute`
      - `pause`
      - `play`
      - `resume`
      - `seek`
      - `stop`
    - Playback information
      - `getAudioBufferDelay`
      - `getDuration`
      - `getMediaPlayerId`
      - `getMute`
      - `getPlayoutVolume`
      - `getPlayPosition`
      - `getPlaySrc`
      - `getPublishSignalVolume`
      - `getState`
      - `getStreamCount`
      - `getStreamInfo`
    - Observers
      - `registerAudioFrameObserver`
      - `registerPlayerSourceObserver`
      - `onAudioVolumeIndication`
      - `onPlayerEvent` (full support not guaranteed)
      - `onPlayerSourceStateChanged` (full support not guaranteed)
      - `onPositionChanged`
      - `onCompleted`
  </details>

- **Changes to makeup feature implementation**: Due to the refactoring of the beauty filter module, calling `setExtensionProperty` to enable makeup effects is no longer supported. Use the `styleMakeup` case of `AgoraVideoEffectNodeId` instead.

#### Issues fixed

This release fixed the following issues.

- Occasional desync between vocals and accompaniment when recording using `startAudioRecording` in a channel.
- Unexpected audio output from the built-in speaker when connecting to a Bluetooth speaker without a microphone and joining the channel as a host.

#### API changes

**New APIs**
- `getAudioBufferDelay`
- `rtcEngine:multiPathStats:`
- `AgoraPathStats`
- `AgoraMultipathStats`
- Added `enableMultipath`, `preferMultipathType` to `AgoraChannelMediaOptions`
- `AgoraMultipathType`
- `AgoraMultipathMode`
- `addOrUpdateVideoEffect`
- `addOrUpdateVideoEffectWithNodeId:templateName:`
- `performVideoEffectActionWithNodeId:actionId:`
- `setVideoEffectFloatParamWithOption:key:floatValue:`
- `setVideoEffectIntParamWithOption:key:intValue:`
- `setVideoEffectBoolParamWithOption:key:boolValue:`
- `getVideoEffectFloatParamWithOption:key:`
- `getVideoEffectIntParamWithOption:key:`
- `getVideoEffectBoolParamWithOption:key:`
- `AgoraVideoEffectObject`
- `AgoraVideoEffectNodeId`
- `AgoraVideoEffectAction`
- `destroy:`
- Added `position` to `AgoraContentInspectModule`
- `preloadEffectEx:soundId:filePath:startPos:`
- `preloadEffectEx:soundId:filePath:`
- New error codes in `AgoraErrorCode`:
  - `AgoraErrorCodePcmSendFormat (200)`
  - `AgoraErrorCodePcmSendBufferOverflow (201)`

</PlatformWrapper>  

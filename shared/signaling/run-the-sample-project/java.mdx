<PlatformWrapper platform="linux-java">


## 3. Integrate the <Vg k="MESS_SDK" />

Refer to the following steps to integrate the <Vg k="MESS_SDK" /> into the sample project:

1. Download the [<Vg k="SIG" />](https://github.com/AgoraIO/Signaling) repository on Github, and locate the `Agora-Signaling-Tutorial-Java` folder.
2.  Download the latest version of [<Vg k="MESS_SDK" /> (Linux Java)](../reference/downloads) and extract the files.
3. Copy the `*.jar` file and the `*.so` file in the SDK folder into the `lib` directory of the project folder.

## 4. Run the sample project

Refer to the following steps to run the sample project:

1. Enter your App ID in `RtmJavaDemo.java`.

    ```java
    public static final String APP_ID = "Your App ID";
	```

2. Install the `.jar` file to local maven.

    ```shell
    $ mvn install:install-file -Dfile=lib/agora_rtm.jar -DgroupId=io.agora.rtm  -DartifactId=agora-rtm-sdk -Dversion=1.0 -Dpackaging=jar
	```

3. Run `mvn package` in the folder of `pom.xml`.

    ```shell
    $ mvn package
	```

4. Run the sample project.

    ```shell
    $ java -cp target/Signaling-Client-Demo-1.0-SNAPSHOT.jar -Dsun.boot.library.path=lib/ io.agora.mainClass.RtmJavaDemo
	```
You see:

![](https://web-cdn.agora.io/docs-files/1619686748194)

</PlatformWrapper>
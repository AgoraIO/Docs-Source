<PlatformWrapper platform="unity">
Override `DestroyEngine` to destroy the track before you destroy the engine:
    ```csharp
    public override void DestroyEngine()
    {
        if (agoraEngine == null)
        {
            return;
        }
        agoraEngine.DestroyCustomVideoTrack(videoTrackID);
        base.DestroyEngine();
    }
    ```
    - <Link to="{{Global.API_REF_UNITY_ROOT}}/class_irtcengine.html#api_irtcengine_destroycustomvideotrack">DestroyCustomVideoTrack</Link>

</PlatformWrapper>
<PlatformWrapper platform="android">

* **Add the screen sharing module to your project**

    1. Download the <Link to="../../sdks">Latest Android <Vpd k="SDK" /></Link> and unzip.

    1. Navigate to the `Agora_Native_SDK_for_Android_FULL/rtc/sdk` folder.

    1. Copy the file named `AgoraScreenShareExtension.aar` into the `<project-name>/app/libs` folder.

    1. In `/Gradle Scripts/build.gradle (Module: <project name>.app)`, add the following line under `dependencies`:

        ```text
            implementation files('libs/AgoraScreenShareExtension.aar')
        ```

</PlatformWrapper>

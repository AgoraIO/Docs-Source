The `MediaNodeFactory` class.

### `create_audio_pcm_data_sender`

Creates a PCM data sender module. This method creates an [`AudioPcmDataSender`](#audiopcmdatasender) object, which can be used in the [`create_custom_audio_track_pcm`](#create_custom_audio_track_pcm) method.

```python
def create_audio_pcm_data_sender(self)
```

#### Returns

- `AudioPcmDataSender` object, if the method call succeeds.
- `None`, if the method call fails.

### `create_audio_encoded_frame_sender`

Creates an encoded audio data sender module. This method creates an [`AudioEncodedFrameSender`](#audioencodedframesender) object, which can be used in the [`create_custom_audio_track_encoded`](#create_custom_audio_track_encoded) method.

```python
def create_audio_encoded_frame_sender(self)
```

#### Returns

- `AudioEncodedFrameSender` object, if the method call succeeds.
- `None`, if the method call fails.

### `create_video_frame_sender`

Creates a YUV video frame sender module. This method creates a [`VideoFrameSender`](#videoframesender) object, which can be used in the [`create_custom_video_track_frame`](#create_custom_video_track_frame) method.

```python
def create_video_frame_sender(self)
```

#### Returns

- `VideoFrameSender` object, if the method call succeeds.
- `None`, if the method call fails.

### `create_video_encoded_image_sender`

Creates an encoded video sender module. This method creates a [`VideoEncodedImageSender`](#videoencodedimagesender) object, which can be used in the [`create_custom_video_track_encoded`](#create_custom_video_track_encoded) method.

```python
def create_video_encoded_image_sender(self)
```

#### Returns

- `VideoEncodedImageSender` object, if the method call succeeds.
- `None`, if the method call fails.

<a name="media-node-factory-release">
### `release`
</a>

Releases the `MediaNodeFactory` object.

```python
def release(self)
```
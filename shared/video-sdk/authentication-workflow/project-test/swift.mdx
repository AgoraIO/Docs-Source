
<PlatformWrapper platform="ios">
3. In the `video-sdk-samples-ios` reference app, open `DocsAppConfig.swift` file and update the values of `appId`,
    `channel`, and `rtcToken` with the values for your temporary token.
</PlatformWrapper>
<PlatformWrapper platform="macos">
3. In the `video-sdk-samples-macos` reference app, open `DocsAppConfig.swift` file and update the values of `appId`,
    `channel`, and `rtcToken` with the values for your temporary token.
</PlatformWrapper>

4. Run your <Vpl k="CLIENT"/>, then wait a few seconds until the installation is complete.

    If this is the first time you run your project, grant microphone and camera access to your app.

5.  **If you are developing with <Vg k="VSDK" />**: enter the same channel name in the UI text box that you used to connect to the <Vg k="COMPANY" /> web demo.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
6. Choose **Broadcaster** to host an event, choose **Audience** to view and listen to a host.

7.  Click **Join** and see the video feed from the web app.
</ProductWrapper>

<ProductWrapper notAllowed="interactive-live-streaming">
6.  Click **Join** and see the video feed from the web app.
</ProductWrapper>


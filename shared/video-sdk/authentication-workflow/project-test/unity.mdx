<PlatformWrapper platform="unity">

3. In your script file, set `_appID` to the value of your app in <Vg k="COMPANY" /> Console.

4. Set `_channelName` and `_token` to empty strings. `_channelName` is obtained from user input in the UI. Your <Vpl k="CLIENT" /> retrieves `_token` from the token server.

5. Update `serverUrl` in the declarations to the base address of your token server. For example, `https://agora-token-service-production-92ff.up.railway.app`.

6. In Unity, click **Play**. You see the <Vpl k="CLIENT" /> running on your device.

<ProductWrapper product={["interactive-live-streaming", "broadcast-streaming"]}>
7. Select a role using the toggle buttons.

8. Set **Channel name** to the name you used in the <Link to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

9. Connect to a channel, click **Join**.
</ProductWrapper>
<ProductWrapper product="video-calling">
7. Set **Channel name** to the name you used in the <Link to="{{Global.DEMO_BASIC_VIDEO_CALL_URL}}"><Vg k="COMPANY" /> web demo</Link>.

8. Connect to a channel, click **Join**.

</ProductWrapper>

</PlatformWrapper>
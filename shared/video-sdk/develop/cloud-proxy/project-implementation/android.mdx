
<PlatformWrapper platform="android">

3. Call `setCloudProxy` and set `proxyType` to `TRANSPORT_TYPE_UDP_PROXY` or `TRANSPORT_TYPE_TCP_PROXY` to enable <Vg k="CP" />.

1. Test if you can initiate audio and video calls or live broadcasts normally.

1. To stop using <Vg k="CP" />, call `setCloudProxy` and set `proxyType` to `TRANSPORT_TYPE_NONE_PROXY`.

1. To update the current `proxyType`, call `setCloudProxy(TRANSPORT_TYPE_NONE_PROXY)` first, then call `setCloudProxy` again with the new `proxyType`.

Call `setCloudProxy` outside the channel. Its settings are valid within the `RtcEngine` life cycle.

</PlatformWrapper>

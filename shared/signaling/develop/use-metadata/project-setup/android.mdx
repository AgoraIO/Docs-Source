<PlatformWrapper platform="android">

1. Open the `build.gradle Module:<project name>` file and ensure that you are using version `1.6.0` or higher of the
  `rtm-special-sdk`. You should have a line similar to the following under `dependencies`:

    ```groovy
    implementation 'io.agora.rtm:rtm-special-sdk:<version>'
    ```

1. Replace `<version>` with the [latest version number](https://central.sonatype.com/artifact/io.agora.rtm/rtm-special-sdk/1.6.0.203/overview).
</PlatformWrapper>
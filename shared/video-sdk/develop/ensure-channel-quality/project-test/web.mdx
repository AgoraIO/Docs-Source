import * as data from '@site/data/variables';

<PlatformWrapper platform="web">

3. In your {props.CLIENT}, Update the `appID` in the declarations to the value from Agora Console.
4. Set the `token` variable to an empty string in the declarations.
5. If you are modifying the *Get Started with {props.PRODUCT} using {data.VSDK}* project, do the follwing:
      1,Update the `serverUrl` in the declarations to the base address of your token server. For example, `\https://app-name.herokuapp.com`.
      2. To start the dev server, execute the following commands in the terminal.

      ```bash
      npm run dev
      ```

      Use the URL displayed in the terminal to open the app in your browser.
      <ProductWrapper notAllowed="video-calling">
      3. Select an option . When you select *Broadcaster*, your local video is published and played in the {props.CLIENT}. When you select *Audience*, your {props.CLIENT} subscribes to and plays to the remote stream.

      </ProductWrapper>

      4. Update the channel name, in the text box at the top, to the same string you used on the demo page and then click *JOIN*.

6. If you are modifying the *Get Started with {props.PRODUCT} using {data.UIK}* project, do the following:

      1. Set the `channel` to the same string you specified in the web demo app. 
      2. Update the `tokenUrl` in the declarations to the base address of your token server. For example, `\https://app-name.herokuapp.com`.
      3. To start the dev server, execute the following commands in the terminal.
      
      ```bash
      cd <app-name>
      npm start
      ```
      
      Use the URL displayed in the terminal to open the app in your browser.
</PlatformWrapper>

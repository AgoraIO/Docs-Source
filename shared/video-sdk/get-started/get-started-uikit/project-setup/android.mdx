<PlatformWrapper platform="android">
1. In _Android Studio_, create a new _Phone and Tablet_ https://developer.android.com/studio/projects/create-project[Android project] with an _Empty Activity_ using Java. Set _Minimum SDK_ to _API 24_.

- After creating the project, Android Studio automatically starts gradle sync. Ensure that the sync succeeds before you continue.

2. Integrate the UIK into your project:

- In `Gradle Scripts/settings.gradle (Project Settings)`, add the `jitpack` repository by adding the following line to the `repositories` section inside `dependencyResolutionManagement`:

```java
  repositories {
  ...
  maven { setUrl("https://jitpack.io") }
  }
```

- In `Gradle Scripts/build.gradle (Module: <projectname>)`, add the UIK dependency. The `dependencies` section should look like the following:

```java
  dependencies {
  ...
  implementation 'ANDROID-UIKIT-SDK:<version>'
  ...
  }
```

- Find the https://jitpack.io/#com.github.AgoraIO-Community/Android-UIK[Latest version of the UIK] and replace `<version>` with the one you want to use. For example: `ANDROID-UIKIT-VERSION`.

3. Sync Gradle and import the UIK.

4. Add permissions for network and device access.

- In `/app/Manifests/AndroidManifest.xml`, add the following permissions after `</application>`:

```xml
 <uses-permission android:name="android.permission.READ_PHONE_STATE"/>
 <uses-permission android:name="android.permission.INTERNET" />
 <uses-permission android:name="android.permission.RECORD_AUDIO" />
 <uses-permission android:name="android.permission.CAMERA" />
 <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
 <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
 <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

 <!-- The Agora SDK requires Bluetooth permissions in case users are using Bluetooth devices.-->
 <uses-permission android:name="android.permission.BLUETOOTH" />
```

</PlatformWrapper>

<PlatformWrapper platform="linux-c">

3. Replace the App ID, channel ID, and token with values from <Vg k="CONSOLE" />.

1. Compile the sample project

    In the terminal, execute the following commands:
    
    ```bash
    cd <extracted-folder>/agora_rtsa_sdk/example
    
    ./build-x86_64.sh
    ```

1. Launch the sample <Vpl k="CLIENT" />:

    To do this:

    1. In the terminal, navigate to the output folder:

        ```bash
        cd out/x86_64
        ```
    
    1. Launch the compiled <Vpl k="CLIENT" />

        ```bash
        ./hello_rtsa --app-id <your-app-id> --channel-id <your-channel-name> --token <authentication-token>
        ```

    You hear an audio file being played and see a video file in the web demo <Vpl k="CLIENT" />. These files are
    pushed from your <Vpl k="CLIENT" />.

</PlatformWrapper>